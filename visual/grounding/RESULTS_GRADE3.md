# Grade-3 honest-reference diagnostic

Status: **PASS** for the deterministic local reference run below.

This is a noncompensatory operational diagnostic. All eight positive axes and
all seven negative controls passed; one failed check would make the complete
report fail. It is **not** a universal solution to symbol grounding, an
adversarial-candidate certificate, or Grade-4 VM isolation.

## Literal lifecycle

1. Recursively commit the candidate package and complete Python SDK.
2. Start the isolated child and send the generic, codebook-independent manifest.
3. Only then materialize the case/codebook through the deferred factory.
4. Deliver all support records and execute both evaluator-owned causal probes.
5. Freeze one checkpoint.
6. Use that same process and checkpoint for every belief, description, motor,
   definition, abstention, and corruption query.

## Reproducible run

```console
python -m grounding_kernel.v1_grade3_benchmark --seed 1 --support-worlds 2
```

Observed accounting:

- support records: 43
- acquisition cost: 8
- sealed logical queries: 14
- motor action cost: 45
- motor resets: 17
- candidate RPC requests before close: 118

Positive results include the true opaque causal belief, externally scored
execution of the withheld fourth role×action cell, reverse description of its
trace, and externally scored execution through the base/middle/chain grounded
definitions.

Negative controls make consequential probe-association shuffles unknown while
preserving an operationally identical shuffle, reject reversed temporal traces,
static/no-sensor input, a fresh symbol, an unannounced surface remap, and both
members of an unanchored recursive definition cycle. Every public trajectory is free of
scalar reward and evaluator outcome codes; pedagogical corrections remain only
on explicitly support-phase turns.

Artifact, SDK, ledger, and report commitments are content-addressed and must be
regenerated after any SDK file changes. The CLI emits the authoritative current
JSON report.
